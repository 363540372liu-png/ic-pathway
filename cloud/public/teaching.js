/* Beginner bridges: additive teaching only; lesson IDs and assessment records stay stable. */
(() => {
const S=(title,...paragraphs)=>({title,paragraphs});
const C=(title,text,...paragraphs)=>({...S(title,...paragraphs),code:{name:title,text}});
const T=(title,heads,rows,...paragraphs)=>({...S(title,...paragraphs),table:{heads,rows}});
const Q=(question,answer)=>({question,answer});
const attach=(id,minutes,bridge,blocks,checkpoint)=>{
 const lesson=window.IC_COURSE.LESSONS.find(l=>l.id===id);
 lesson.minutes=minutes;lesson.teaching={bridge,blocks,checkpoint};
};
attach('testbench',65,
'第9课写出计数器，第10课用状态机规定“这一拍之后做什么”。现在继续使用同一个计数器：设计已经写好，但我们还没有证据证明它按规则工作。这一课给它搭一套能重复做实验的环境，不需要先学一套全新的电路。',[
S('1 · 从你已经会的计数器开始',
'回忆计数器的三个输入：clk 是采样节拍；rst_n=0 表示要求清零；en=1 表示允许加一。count 是输出。设计里的 always @(posedge clk) 只规定“上升沿到来时怎样更新”，并不负责自己创造上升沿。若没有外部输入，写好的模块不会自己演示计数。',
'在板卡上，时钟可能来自振荡器，控制信号来自按钮处理电路或其他模块。在软件仿真中，这些输入由测试台生成。测试台还负责看输出、和预期对照。因此它像实验人员加仪器；被测试的计数器才是实验对象。DUT 是 Design Under Test 的缩写，意思就是“被测设计”。'),
C('2 · 先确认实验对象：counter.v（可综合 RTL）',`\x60timescale 1ns/1ps
module counter #(parameter WIDTH=8) (
  input wire clk, rst_n, en,
  output reg [WIDTH-1:0] count
);
  always @(posedge clk) begin
    if (!rst_n) count <= {WIDTH{1'b0}};
    else if (en) count <= count + 1'b1;
  end
endmodule`,
'这就是第9课的二进制计数器。WIDTH 决定有多少位，默认8位。测试时会选4位，因此输出范围是0～15，15再加一回到0。它不是“数到9就回零”的十进制计数器；测试预期必须服从正在测试的那份规格。',
'端口是模块与外界接线的位置。input/output 表示从这个模块的角度看信号方向。输出声明为 reg，是因为它在过程块里赋值；这里真正让它表示寄存器的是上升沿更新行为。没有 en 分支赋值时保持原值。'),
C('3 · 模块名、实例名和接线名，逐个拆开',`// tb_counter 内部片段：不是完整文件
reg tb_clk, tb_rst_n, tb_en;
wire [3:0] observed_count;
counter #(.WIDTH(4)) dut (
  .clk(tb_clk),
  .rst_n(tb_rst_n),
  .en(tb_en),
  .count(observed_count)
);`,
'counter 是模块定义的名字，相当于一张可以重复使用的图纸。dut 是这一次放入测试台的实例名字，可以改成 u_counter。tb_counter 则是外层测试模块的名字。三者不需要同名：测试台中放了一份名叫 dut 的 counter。',
'#(.WIDTH(4)) 在建立这个实例时把参数设为4，它不是“等待4个时间单位”。稍后看到的 #5 才是过程里的时间延迟。两种语法看起来都有 #，位置和含义不同。',
'.clk(tb_clk) 左边的 clk 是 counter 定义中的端口，右边的 tb_clk 是外层测试台的信号。读作“把测试台的 tb_clk 接到计数器的 clk 端口”。.count(observed_count) 则把计数器输出接回测试台。名字不一样仍然能连；没有连线，不会因为名称相似就自动相连。',
'测试台用 reg 保存由 initial/always 改写的输入，用 wire 接收子模块驱动的输出。这里 reg 是仿真变量，不代表测试台会被做成寄存器芯片。判断是否描述实际硬件要看代码用途和综合支持，不能只看一个类型名。'),
S('4 · 仿真时间不是你电脑的墙上时间',
'仿真器按事件推进一个虚拟时钟。例如“现在是20 ns”表示被模拟电路的时间坐标，不是程序真的只运行了20 ns。你的电脑可能花一秒计算很长一段电路行为。',
'`timescale 1ns/1ps 中，1ns 是本文件的延迟单位，1ps 是时间精度。因此 #5 在这里等待5 ns，#0.5 等待0.5 ns；精度决定更细的小数延迟怎样取整。本课所有相关文件使用相同声明，先避免跨文件时间单位混淆。'),
C('5 · 单独生成时钟：为什么 #5 对应10 ns周期',`// 仅用于测试台；已有 reg tb_clk = 0;
always #5 tb_clk = ~tb_clk;`,
'always 让这一过程反复执行；#5 让它每次先等待5 ns；~ 对一位时钟取反，0变1、1变0。初始为0时，5 ns变1，10 ns变0，15 ns再变1。两个相邻上升沿之间是10 ns，所以周期10 ns、频率100 MHz。',
'一定要给时钟明确初值。如果从 X 开始，~X 仍是 X，时钟就不会产生你想要的0/1边沿。也不要写没有任何等待的 always tb_clk=~tb_clk：它不断在同一仿真时刻执行，时间无法正常前进。',
'设计的 always @(posedge clk) 等待外部时钟边沿；测试台的 always #5 在虚拟时间里制造时钟。这是两种不同工作，后者不能作为本课待综合电路的时钟源。'),
C('6 · 一个过程做一次实验，另一个过程一直给时钟',`// 仅用于测试台，替换原激励 initial，不要叠加驱动
initial begin
  tb_rst_n = 0;
  tb_en = 0;
  repeat (2) @(negedge tb_clk);
  tb_rst_n = 1;
  tb_en = 1;
  repeat (5) @(negedge tb_clk);
  $display("count=%0d", observed_count);
  $finish;
end`,
'initial 中的过程从仿真开始执行一次。“一次”不是只执行一行，而是按顺序执行里面所有语句，遇到等待就暂停，条件满足后接着往下走。它与生成时钟的 always 同时存在：激励块正在等待边沿时，时钟块仍然会翻转信号。',
'@(negedge tb_clk) 表示等待下一次下降沿；repeat (2) 表示把紧跟着的等待执行两次。这里等到10 ns和20 ns两个下降沿后，才继续解除复位。同步复位必须跨过上升沿才作用于 count，因此“把复位写成0，马上又写成1”是不够的。',
'为什么选择下降沿改输入？DUT 在上升沿采样，错开半个周期能让下一上升沿之前的输入稳定，避免测试台修改和电路采样同时发生。本课使用这个容易手算的约定。它不是说真实系统只能在下降沿改变输入。',
'$display 把数值打印到日志。%0d 是十进制显示位置，后面的 observed_count 填到这个位置；$finish 结束整个仿真，否则产生时钟的 always 会一直运行。打印一个数只完成了观察，还没有自动判断是否正确；第13课才把判断系统化。'),
T('7 · 不运行工具，也先把这次实验手算出来',
['时刻（ns）','发生的事','count（事件处理完成后）'],[
['0','clk=0，复位有效，尚无上升沿','X：未经过同步复位采样'],
['5、15','上升沿，rst_n=0','0'],['10、20','两次下降沿；20 ns解除复位、使能','0'],
['25','第一次允许计数的上升沿','1'],['35、45、55、65','继续经过四次计数上升沿','2、3、4、5'],
['30、40、50、60、70','使能之后等待的五个下降沿','70 ns打印5，然后结束']],
'注意 repeat(5) 等的是五个下降沿，不是五个单位时间。虽然打印在下降沿，count 的更新仍然来自它之前的上升沿。可以先把上升沿和下降沿分别列出来，再数“解除复位之后”经历了几个有效上升沿。'),
C('8 · 拼成两个文件：tb_counter.sv（仅仿真）',`\x60timescale 1ns/1ps
module tb_counter;
  reg tb_clk = 0;
  reg tb_rst_n = 0;
  reg tb_en = 0;
  wire [3:0] observed_count;
  counter #(.WIDTH(4)) dut (
    .clk(tb_clk), .rst_n(tb_rst_n),
    .en(tb_en), .count(observed_count)
  );
  always #5 tb_clk = ~tb_clk;
  initial begin
    repeat (2) @(negedge tb_clk);
    tb_rst_n = 1; tb_en = 1;
    repeat (5) @(negedge tb_clk);
    $display("count=%0d", observed_count);
    $finish;
  end
endmodule`,
'将上面的 counter 模块保存为 counter.v，将这份测试台保存为 tb_counter.sv。测试台通常没有端口：它在自己的内部生成信号，包住被测电路，因此是这次仿真的顶层。仿真时同时读入两份文件，选择 tb_counter；综合时只选择 counter 及它需要的设计文件。',
'这份测试台预期打印 count=5，但网页没有执行它。第14课带你在工具里得到真实输出。先完成一个小改动：把最后 repeat(5) 改成 repeat(3)，预测打印3；再解释为什么 count 不是在 initial 中直接写出来的。'),
S('9 · 卡住时，按输入到输出检查',
'一直是 X：先看时钟是否初始化并翻转，再看同步复位是否经过上升沿，最后查端口是否连对。一直是0：看是否一直保持复位、en是否真的为1。找不到模块：检查两个源文件是否都读入，而不是随意改测试预期。',
'原有练习使用 .clk(clk) 这样的同名接线，与本课的 .clk(tb_clk) 是同一种连接规则。看到不同名称时，先画出外层信号到内层端口的对应关系，就不会把实例化误认为函数调用。')],
Q('不用看代码，说明谁产生时钟、谁执行加一、谁读取结果。然后解释 .count(observed_count)。','测试台产生时钟和控制信号；counter实例在上升沿按规格更新；测试台读取输出。count是子模块端口，observed_count是外层接收它的信号，两者通过命名端口连接在一起。'));

attach('waveforms',55,
'第11课已经搭好实验，但最后打印5不能告诉你中途有没有出错。这一课把同一段实验展开成时间线，观察每一次输入和输出变化。先会读一条信号，再读几条信号之间的因果关系。',[
S('1 · 波形是一份随时间变化的记录',
'横轴是仿真时间，向右表示更晚；每一行是一条信号。单比特通常画成高低电平，高是1、低是0，竖直变化处是边沿。多比特总线不是“更高的电压”：显示的数字代表整组位，可按二进制、十六进制或十进制查看。',
'例如四位 count 的 1010、十六进制A、无符号十进制10是同一组位。若看见a而你预期10，先核对显示进制；不要立刻认为电路数错。缩放只改变观察范围，不会重新运行电路。'),
S('2 · 为什么刚开始会有 X',
'X 表示仿真器不能确定该位是0还是1。第11课 count 没有初始化，且使用同步复位，所以在第一个上升沿到来前显示 X 是可以解释的；第一个有效复位边沿之后应变为0。若复位之后仍是X，就需要排查。',
'Z 表示高阻，常用于未驱动网络或三态接口，不等于0；X也不是硬件中一个叫“未知”的稳定电压值。四态模型用这些符号帮助暴露未初始化、断线或驱动冲突等问题。首先问“它从哪一时刻出现”，再追查来源。'),
T('3 · 先看边沿之前，再看边沿之后',['上升沿前','在上升沿采样','更新后的 count'],[
['count=2，rst_n=1，en=1','允许加一','3'],['count=3，rst_n=1，en=0','保持','3'],['count=3，rst_n=0，en=1','复位优先','0']],
'读波形时先定位 clk 上升沿，读这个边沿前的复位和使能，再根据规则推导输出。en在两个上升沿之间变了，不代表count立即变化。波形图上看似同一个时间坐标的事件，还可能属于仿真器在该时刻的不同处理阶段。'),
C('4 · 为什么刚等到上升沿，可能读到旧值',`// 测试台观察片段：10 ns周期，1ns/1ps
@(posedge clk);
#1;
$display("count=%0d", count);`,
'第8课的 <= 会先根据当前值计算右侧，再在该时刻稍后的非阻塞赋值更新阶段写入左侧。测试台从 @(posedge clk) 返回时，DUT可能刚安排好更新，却还没有真正更新count。此时立即打印可能看见旧值。',
'本课程简单测试在上升沿之后等待1 ns再读，让零延时RTL更新完成。这里的 #1 是教学采样安排，不是计数器真实的1 ns传播延迟。它只适用于当前10 ns周期等明确条件；更快时钟或带延时门级仿真需重新设计采样方案。'),
S('5 · 竞态：同一时刻谁先执行不应决定测试结果',
'若测试台在上升沿用 en=1 改输入，DUT也在同一上升沿读en，不同过程之间的执行顺序可能影响读到新值还是旧值。这叫竞态。它与刚才“等待非阻塞更新再看输出”相关，但不是同一个动作：一个关乎输入何时驱动，一个关乎输出何时采样。',
'本课统一用下降沿改输入，上升沿后稍等再检查。先建立能稳定解释的测试习惯，不必现在记住所有事件调度区域。将来学习SystemVerilog验证时，再用专门的驱动与采样机制深化。'),
C('6 · 把信号写到文件，才能用波形工具看',`// 加入 tb_counter，独立记录块；仅仿真
initial begin
  $dumpfile("counter.vcd");
  $dumpvars(0, tb_counter);
end`,
'$dumpfile 指定波形文件名；$dumpvars 的0表示向下记录所选范围的所有层级，tb_counter选择本次测试顶层。如果你的顶层名字不同，应随之调整。这个initial负责记录，不要在其中再驱动另一份复位或使能。',
'仿真运行后才生成数据。GTKWave读取VCD并显示信号，不能替你运行刚修改的代码。打开后先加入 clk、rst_n、en、count，再缩放到解除复位附近；如果采用第11课异名版本，则选择tb_clk等相应信号。'),
S('7 · 用第一次错误缩小范围',
'假设25 ns之后应为1，却仍为0。向左查看20 ns时rst_n和en有没有变为1；再看25 ns是否确实是上升沿；最后查DUT内部端口与外层信号是否一致。先修最早的分歧，后面错误可能只是它的结果。',
'练习时把en关闭两拍再恢复。先在纸上写出“1、2、2、2、3”，再设计对应输入并看波形。你应能解释每个保持段对应哪个控制条件，而不是只说曲线看着正常。')],
Q('为什么0 ns的 X 可能合理，而同步复位上升沿之后的 X 值得排查？','同步复位必须等上升沿执行。之前寄存器可能未初始化；复位边沿完成更新后应有确定的0，继续出现X说明要查时钟、复位、连接或驱动问题。'));

attach('selfcheck',60,
'第12课让你会沿波形找错误。现在把你手算“这一拍应该是多少”的过程写进测试台，让仿真器自动比较；波形仍用来解释失败原因。先检查一次，再推广到多拍循环。',[
S('1 · 三件事分开：激励、预期、实际',
'激励是你给电路的输入，例如复位、使能和时钟。实际值是从DUT输出读到的count。预期值是你根据规格独立算出的答案，保存在测试台变量expected里。expected不是设计端口，也不负责驱动count。',
'若写 expected=count 再比较，两边当然相等，却没有验证规则。正确顺序是先根据旧预期和本拍输入算新预期，再等DUT更新，最后比较。测试台可以用适合表达规格的整数运算，不必照抄电路实现。'),
C('2 · 从一个明确的断言开始（测试台片段）',`// 4位count；测试台已有时钟、复位和DUT
@(negedge clk); rst_n=0; en=0;
@(posedge clk); #1;
if (count !== 4'd0)
  $fatal(1, "reset failed: count=%b", count);`,
'第一行在下降沿施加复位；第二行等待它真正被上升沿采到，再等待更新完成；第三行问“现在是不是确定的0000”。如果不是，就用$fatal报错并结束。这里使用 !==，因为它能把X/Z也纳入比较并返回确定的是或否。',
'普通 != 在比较中遇到未知值可能返回X，而if不会把X视为明确的真，错误可能被漏报。4\'d0明确预期为4位零；%b打印位模式，便于发现哪一位未知。'),
C('3 · 再检查一次加一',`// 仅仿真：放在同一个initial中，已完成复位检查
expected = 0; // integer expected 需在模块内声明
@(negedge clk); rst_n=1; en=1;
expected = (expected + 1) % 16;
@(posedge clk); #1;
if (count !== expected[3:0])
  $fatal(1, "expected=%0d actual=%0d", expected, count);`,
'%是取余数。4位无符号计数器有16种状态，用加一后对16取余表达回绕：14变15，15变0。expected采用integer便于写规格模型，比较时取低4位以匹配count。若改成十进制计数器，模型必须改为对10取余，不能照搬16。',
'这段代码在下一个上升沿之前就算出了预期1，但没有要求电路提前输出1；直到上升沿后的检查点才比较。把“何时算答案”和“何时检查电路”分开，是理解测试的重要一步。'),
T('4 · 从规格列出最小测试计划',['测试目的','给什么输入','在何时检查什么'],[
['启动复位','rst_n=0，跨过上升沿','更新后count=0'],['连续计数','解除复位，en=1','每拍分别为1、2、3…'],['暂停保持','en=0，两拍','两拍都保持暂停前数值'],['边界回绕','从0连续使能16拍','第15拍15，第16拍0'],['运行中复位','计到非零再rst_n=0','下个上升沿后变0'],['优先级','rst_n=0且en=1','清零而不是加一']],
'“测试复位”太笼统，要同时写激励、观察时刻和预期。只看16拍后的0可能错过中间跳数，因此每拍比较比只看最终值更有力。'),
C('5 · 完整的小型自检：与第11课 counter.v 配套，仅仿真',`\x60timescale 1ns/1ps
module tb_counter_check;
  reg clk=0, rst_n=0, en=0;
  wire [3:0] count;
  integer expected=0;
  integer i;
  counter #(.WIDTH(4)) dut(
    .clk(clk), .rst_n(rst_n), .en(en), .count(count));
  always #5 clk=~clk;
  initial begin
    @(negedge clk); rst_n=0; en=0;
    @(posedge clk); #1;
    if(count !== 4'd0) $fatal(1,"reset failed");
    for(i=0; i<16; i=i+1) begin
      @(negedge clk); rst_n=1; en=1;
      expected=(expected+1)%16;
      @(posedge clk); #1;
      if(count !== expected[3:0])
        $fatal(1,"step=%0d expected=%0d got=%0d",
               i+1, expected, count);
    end
    $display("PASS: reset and 16 increments");
    $finish;
  end
  initial begin
    #1000; $fatal(1,"timeout");
  end
endmodule`,
'for括号里的三部分是：i从0开始；只要i<16就继续；每轮完成后i加一。因此执行i=0到15共16轮。begin/end把每轮的等待、驱动和检查绑在一起。i+1用于输出人习惯的第1到16次检查。',
'第二个initial与第一个并发，充当超时看门狗。正常测试会在1000 ns之前执行$finish并结束整个仿真；若时钟不再产生、主测试一直等待，它才用$fatal报告超时。不要把看门狗写在永远等不到的语句后面。',
'这份完整示例只覆盖启动复位和连续计数回绕，尚未覆盖暂停及运行中复位。它的PASS文字明确写出范围。后面的独立练习请把缺失用例补上，不要把这个PASS当作全部功能证明。'),
S('6 · 让错误真的被抓到',
'在练习副本中把DUT的加一改成加二，然后重新编译运行。第一次计数检查应得到expected=1、actual=2并失败。若仍PASS，检查是否编译了错误文件、跑了旧产物，或测试没有连到这个DUT。完成后恢复正确设计。',
'失败信息写清“第几拍、输入、预期、实际”比只打印ERROR有用。保存首次失败，用波形回到那个边沿；修复后把这个用例留在测试中，再运行其他旧用例，这就是回归的起点。')],
Q('为什么这份自检的PASS还不能证明en=0时能保持？','它的计数循环始终en=1，没有执行暂停用例。需要先计到非零，在下降沿关闭en，跨过至少两个上升沿并逐拍确认保持，才能增加这一条证据。'));

attach('local-tools',55,
'你已经能读测试台和手算预期。这一课让电脑真正执行同一份实验。先理解每个文件从哪里来、交给谁、产出什么，再输入命令；命令选项只是告诉工具这些信息。',[
S('1 · 编辑器、仿真器、波形查看器各做一件事',
'编辑器把你写的文本保存到磁盘，通常不会仅因为保存成功就执行Verilog。仿真器读入设计和测试台、建立模块连接，再按事件推进仿真。波形查看器读取已记录的数据，帮助你观察。三者可以在同一套软件里出现，也可以是三个独立程序。',
'本课用Icarus读取并编译，用vvp运行编译产物，用GTKWave查看VCD。先运行工具版本命令确认安装；找到命令但编译报错，和根本找不到命令，是两种不同问题。'),
T('2 · 最小实验目录里应该有什么',['文件/目录','谁创建','作用'],[
['rtl/counter.v','你从第11课保存','可综合计数器'],['tb/tb_counter.sv','你从第11课保存','生成输入、打印输出'],['counter_sim','iverilog生成','交给vvp的编译产物'],['counter.vcd','带dump语句的测试台运行生成','交给GTKWave的波形']],
'先创建rtl和tb两个文件夹，再保存对应源文件。确认文件实际没有多出.txt后缀。project根目录就是同时包含rtl和tb的那层文件夹。下面命令都假设终端已经位于这里。'),
C('3 · 第一轮只求得到一个真实的5',`iverilog -g2012 -s tb_counter -o counter_sim rtl/counter.v tb/tb_counter.sv
vvp counter_sim`,
'第一行的iverilog是程序名，后面的内容是给它的参数。-g2012选择语言支持模式；-s后面是测试顶层模块名tb_counter，不是文件名；-o后面是输出文件名counter_sim；最后两个路径是需要读入的源码。只编译测试台却没有counter定义，会找不到DUT。',
'确认第一行成功后，再单独执行第二行。vvp读取counter_sim并推进仿真；第11课原始测试预期打印count=5并结束。它没有自动自检，所以看见5后仍要和手算结果对照。若使用第13课的完整自检，顶层与文件名要相应换成tb_counter_check及你保存的文件。'),
S('4 · 再增加波形，不要一次改变所有东西',
'得到正确文本输出后，再把第12课dump记录块加进测试台，重新编译、重新运行，然后打开counter.vcd。没有dump记录块时，不应期待仿真器自动产生这个VCD。',
'在GTKWave里展开测试顶层，选取时钟、复位、使能和count，加入显示区域。放大20～35 ns附近，看解除复位与第一次计数。能从日志和波形解释同一次运行，才完成这一小步。'),
S('5 · 错误信息先分清发生在哪一步',
'command not found说明终端找不到工具，应查安装或PATH；No such file说明给出的路径不存在，应查当前目录和文件名；syntax error才需要回到相应源码行。不要为修复环境问题随意修改RTL。',
'编译失败后旧counter_sim可能仍存在。直接运行它会得到上一次的结果，造成“改错了还PASS”的错觉。每次明确确认本次编译成功，再继续。保存终端完整命令和首个错误，便于复现。'),
S('6 · 学到脚本前先把单步流程走通',
'下面原有代码练习会用Bash自动串起命令。Bash是一种命令解释器，和Verilog不同，脚本里的每一行是在安排软件工具工作，不是在描述电路。Windows原生终端不一定直接支持bash，应使用你已配置的Bash/WSL或学校Linux环境。',
'暂时没有Bash也可以先逐条完成上面的两条工具命令。不要为了学仿真同时强迫自己学完整Linux。先记录工具版本、当前目录、两份源码、实际输出；下一课再把路径和环境系统地讲清楚。')],
Q('改完counter.v后只重新打开VCD，为什么看不到新行为？','VCD是上次运行留下的记录。需要重新编译当前源码，再运行新的产物生成新波形，最后打开或重新加载它。'));

attach('linux-vcs',50,
'第14课在本地完成实验；学校机器上仍然是同一组源码、编译和运行，只是文件位置与软件环境不同。本课先建立“我在哪个目录、文件在哪、调用哪个程序”的判断习惯。',[
S('1 · 终端是一种操作文件和程序的入口',
'终端里输入命令，命令解释器负责启动程序或执行内置动作。提示符前常显示用户、主机或目录；提示符本身通常不用输入。键入pwd回车，才是在询问“当前工作目录是什么”。',
'连接远程学校机器后，看到的文件属于那台机器，不是你电脑下载文件夹。把文件下载到本机不等于学校机器已拥有它；按学校允许的上传方式传到自己的目录，再核对。'),
C('2 · 用一组具体目录理解相对路径',`pwd
ls
mkdir -p work/vcs
cd work/vcs
pwd
ls ../../rtl/counter.v`,
'假设开始在/home/student/ic-lab。mkdir -p work/vcs创建两层输出目录，cd切到里面后，当前位置是/home/student/ic-lab/work/vcs。一个..回上一层work，第二个..回到ic-lab，所以../../rtl/counter.v指向设计文件。',
'绝对路径从根目录/开始；相对路径从当前目录开始。同一条../../rtl/counter.v在不同目录执行可能指向不同地方。命令报找不到文件时，先pwd，再ls核对，不要先改模块名。这里的路径是教学例子，不是学校真实账号目录。'),
S('3 · 文件存在、工具可找到、许可可用，是三件事',
'ls确认源码是否在指定位置。command -v vcs确认当前命令环境能否定位VCS程序。vcs -help能显示帮助，也仍不等于一份真实设计已经编译或许可完全可用。最后还需要实际编译运行。',
'PATH是查找命令所用的目录列表；学校可能要求先加载module或source环境脚本，设置工具和许可变量。按老师给出的命令操作，不要从别人的电脑照抄不明路径或修改许可配置。'),
S('4 · shell变量和Verilog信号不是一回事',
'后续脚本中的$STDCELL_DB属于命令环境，保存的是库文件路径；Verilog里的rst_n属于设计信号。它们运行于不同系统。source某个脚本是在当前shell执行环境设置，不是把一个Verilog模块加入设计。',
'这一阶段只要求你能按学校说明准备环境并知道失败属于哪一层。SSH主机名、账号、密码、VPN和授权安排来自学校；课程页面不会替你建立这些连接。'),
S('5 · 小练习：在运行前给自己一个可核对的起点',
'创建自己的工作目录，写下pwd输出，确认两份源码可见，再记录command -v vcs结果。若卡在权限或许可，就标为待解决并保留错误；已经掌握的RTL与本地仿真仍可继续练习。',
'求助时提供工具版本、当前目录、完整命令和必要错误片段；不要公开密码、令牌或内部许可地址。能让别人复现问题的上下文，远比一张只截最后一行的截图有用。')],
Q('源码明明存在，为什么工具仍说找不到../../rtl/counter.v？','相对路径依赖运行位置。先检查pwd，从当前目录逐层解析..，确认文件路径；不能仅凭文件在某处存在就认为当前命令引用了它。'));

attach('vcs-run',50,
'已经理解本地编译与运行，现在把同一份实验交给学校VCS。关键是保留相同设计、参数和测试预期，通过文件列表明确告诉工具“这次到底读了哪些文件”。',[
S('1 · 先把“编译”再拆开看',
'工具首先读语法和模块定义，再根据测试顶层找到实例、设置参数、连接端口，这个建立具体设计层次的过程叫展开。counter定义里默认8位，但tb_counter的实例指定4位，所以这次仿真展开出4位DUT。',
'然后VCS生成可运行的仿真程序。编译能发现模块不存在、端口拼错等问题，却不会仅凭编译成功就替你执行所有输入场景。运行程序之后，initial和always才按仿真时间工作。'),
C('2 · 文件列表只是源文件路径清单',`# filelist.f 的两行内容（按在 work/vcs 中运行的约定）
../../rtl/counter.v
../../tb/tb_counter.sv`,
'filelist.f不是Verilog，也不是自动生成时钟的文件。它把本次源文件集合固定下来，避免手动命令漏掉一份源码。工具会按照运行目录及自身规则解释路径，本课固定从work/vcs运行。',
'修改文件前核对列表指向的路径。文件夹里有两份同名counter.v时，编辑A而编译B是常见错误。不要只看文件名相同就认为是同一份文件。'),
C('3 · 读懂两条命令的输入和输出',`# 在学校已配置VCS的 work/vcs 目录，逐条执行
vcs -full64 -sverilog -top tb_counter -f ../../filelist.f -o simv -l compile.log
./simv -l sim.log`,
'-f指定刚才的清单；-top指定仿真顶层；-o指定生成程序名。simv是这里选定的名字，可以更换，但运行时也必须对应。./表示当前目录，因此./simv是在运行这里刚生成的程序。其余选项和版本支持以学校vcs -help为准。',
'compile.log记录编译过程；sim.log记录运行时的打印、失败或结束信息。counter.vcd由测试台的记录语句生成。看到compile.log存在不等于编译成功，看到sim.log存在也不等于全部检查通过，要读内容和退出状态。'),
S('4 · 如何公平比较两个仿真器',
'保持两边RTL、测试台、顶层、参数和激励一致，再比较自检结论与关键波形。不要一边测4位计数器，另一边测8位，却把不同回绕时间理解为仿真器出错。',
'若出现差异，先查文件列表、语言模式、timescale和采样竞态。某工具“刚好”跑出预期，不能证明依赖过程执行顺序的测试写法可靠。回到下降沿驱动、正确时刻采样的明确约定。'),
S('5 · 这次运行应该交付什么',
'至少保存源码位置、工具版本、实际编译命令、编译是否成功、运行日志、预期与实际结果。第11课基础实验预期count=5，第13课自检则有明确PASS范围；不要把两者混为一种验收。',
'正常结束却没有预期日志时，检查是否选择了错误顶层、是否提前$finish、是否仍运行旧产物。一直不结束时，检查时钟和等待条件，使用测试台看门狗暴露无限等待。')],
Q('为什么源文件中有tb_counter，但-top写成counter会丢失这次实验？','以counter为顶层时，测试台不在它的下层，不会作为外部驱动环境自动包住它。需要从tb_counter展开，才能执行其中的时钟、激励和检查。'));

attach('debug',50,
'第13课已经让测试发现错误，第16课能保留实际运行记录。这一课练习怎样把一个“FAIL”缩小为某个输入、某个边沿、某一条规则，而不是同时乱改多处代码。',[
T('1 · 先判断错误在哪一层',['现象','先检查','暂时别做'],[
['找不到vcs、许可失败','命令环境和学校授权','改计数算法'],['找不到文件、未知模块','当前目录、文件列表、顶层','随便加延时'],['语法/端口错误','首个错误行及相邻代码、接口','继续运行旧程序'],['实际值不等于预期','首个失败边沿和规格','删除检查'],['一直不退出','时钟、等待、结束条件','只让程序再跑更久']],
'日志可能连续报几十条错，其中很多由第一个错误引起。例如缺少end可能让后面的合法代码全被误读。先处理第一个有意义的错误，再重新编译获取更新后的信息。'),
S('2 · 把第一次分歧写成可手算的小问题',
'例如第4次有效计数后expected=4、actual=3。先检查这四次上升沿前en是否都为1、rst_n是否都为1，再看参考模型是否多算了一拍。测试台本身也可能错，不能看到FAIL就断定DUT错。',
'把失败前两三个边沿的输入、旧count和预期列成表。如果恰好在暂停恢复时少一拍，就集中查使能切换与采样时刻；不需要立刻重写整个模块。'),
C('3 · 让失败信息足够定位（仅测试台）',`if (count !== expected[3:0])
  $fatal(1, "t=%0t rst_n=%b en=%b expected=%0d actual=%0d",
         $time, rst_n, en, expected, count);`,
'$time提供仿真时刻，%b适合控制位，%0d适合数值。格式占位顺序要和后面的参数顺序一致。保留失败向量，必要时同时用%b输出count来观察未知位。',
'最小复现不是删掉检查让它通过，而是保留能触发同一失败的最短实验。在独立副本缩短输入序列，原始失败日志和源码先保留，方便判断修复前后是否在比较同一问题。'),
S('4 · 修复、回归和故障注入分别解决什么',
'修复改变设计或测试，使它符合已确认的规格。回归重新运行已有用例，确认这次改动没有破坏原本正确的行为。故障注入则故意在练习副本制造明显错误，检验测试有没有能力发现它。',
'例如修好“暂停后恢复少一拍”，应重新跑启动复位、连续计数、暂停、回绕和运行中复位，而不是只保留恢复用例。失败案例应成为以后固定执行的一部分。'),
S('5 · 脚本是把成功与失败可靠传出去',
'命令退出状态通常用0表示成功、非0表示失败。Bash中的set -euo pipefail有助于暴露常见命令失败、未定义变量及管道中的失败；tee把输出一边显示一边写文件。它们不是Verilog语法。',
'这些选项有shell语义和适用边界，仍需核对实际工具是否正确返回错误状态。脚本跑完、生成文件、打印PASS是不同层次的证据。测试台应真正检查预期并用$fatal报错，脚本应保留这个失败，而不是覆盖退出状态。')],
Q('把暂停用例删掉后全PASS，算修复吗？','不算。检查范围缩小了，原来的失败没有被解释或修复。应保留用例，定位到规格、DUT或测试台中的具体错误，再重跑它和其他旧用例。'));

attach('synthesis',55,
'前面验证的是“给定输入时，RTL描述的行为是否正确”。现在要回答另一个问题：用实际可用的门和触发器怎样实现这个行为？综合承接你已经会的组合逻辑、寄存器和状态机。',[
S('1 · 从熟悉的代码看硬件',
'assign y=a&b描述与运算，综合器可以选择实现该功能的单元。always @(posedge clk)描述边沿触发的存储；if(en) q<=d而没有else表示保持，因此输入路径需要实现“新d还是旧q”的选择。先能说明功能结构，再学习工具命令。',
'源码是行为描述，不是每行都对应一个独立硬件盒子。综合器可以合并重复逻辑、传播常量、删除不影响输出的逻辑，或者使用库中带使能功能的单元。优化后的结构不同，不自动表示功能变了。'),
C('2 · 把一位寄存器的下一状态写出来（可综合片段）',`// 放在已有clk、rst_n、en、d、reg q的模块中
always @(posedge clk) begin
  if (!rst_n) q <= 1'b0;
  else if (en) q <= d;
end`,
'复位有效时，下一状态选0；否则使能有效时选d；否则选当前q。存储由触发器承担，选择由组合功能承担。这是你可以预测的结构，但最终可能映射成一个带专用控制端的触发器，也可能是选择逻辑加触发器，要看目标库和优化。'),
T('3 · 综合需要什么，交付什么',['角色','内容','缺少会怎样'],[
['输入：RTL与顶层','模块、连接、参数','不知道实现哪个电路'],['输入：目标库','可用单元及其模型','无法按该工艺映射和估计'],['输入：约束','时钟和外部接口预算','优化与时序结论缺少目标'],['输出：网表','具体单元实例及连接','用于后续实现/检查'],['输出：报告','结构问题、面积、时序','用于判断结果与条件']],
'仿真测试台中的#5、输入激励和$finish用于安排实验，不属于待实现的counter电路。综合顶层应是counter，而不是tb_counter。'),
S('4 · 映射与网表，不再只是一段公式',
'映射是把逻辑功能落到目标中可用的资源。ASIC中是标准单元，FPGA中可能是LUT、触发器和片上存储等资源。共同概念是“用目标提供的元件实现RTL”，但两者资源、库和后续物理流程不同。',
'网表记录实例和连接关系，可以理解为逻辑层面的元件清单及接线。它还不是制造好的芯片，也不是完整布线图。后续仍有布局布线、提取与相应检查。'),
S('5 · 功能正确与速度足够要分别验证',
'计数器仿真每拍加一正确，并不代表组合路径一定能在目标周期内稳定。功能仿真回答逻辑规则，时序分析回答信号是否及时到达；面积报告回答在特定库模型下占用多少资源。不能用一个PASS代替全部结论。',
'现在先练习预测“有没有寄存器、什么条件选择下一状态、哪些逻辑可能被常量化”。等第19课明确库、第20课明确时间预算，再看第21课的实际报告。')],
Q('同步复位和使能为什么会影响寄存器输入侧的逻辑？','它们决定下个上升沿要保存0、新数据还是旧状态。具体实现可能是输入选择逻辑或库中带相应功能的单元，不能只按代码行数确定门的数量。'));

attach('libraries',55,
'第18课知道综合要挑选真实元件。这里先认识元件库是什么，再理解“选择可用元件”和“找到已引用模块”这两项工作，最后读对应的DC脚本。',[
S('1 · 标准单元库是带参数的元件目录',
'库不仅说有AND、MUX、DFF，还提供逻辑功能、面积和相关时序/电气模型。同一逻辑功能可能有多种驱动强度：较强单元可能驱动负载更快，也可能占更大面积。综合器根据条件做取舍，而不是所有AND都完全相同。',
'工艺、电压和温度会改变元件行为。工艺角是库所代表的一组条件模型，不是芯片的几何角落。比较报告时要记录使用的库及条件，否则“更快、更小”的数字可能只是换了分析前提。'),
S('2 · target和link为什么分开',
'target_library列出综合映射可以选择的目标标准单元库。link_library帮助工具解析设计中已引用的模块、宏和单元。前者侧重“可选什么来实现”，后者侧重“这个被引用的名字在哪里有定义”。',
'本课小项目常把目标库也放进link列表，并用*搜索已读入设计。更复杂的IP或宏需要对应模型，但现在不要求你自行配置复杂多库系统。先保证普通counter能展开、引用能解析。'),
C('3 · 先学这几种Tcl写法，再读DC命令',`# Tcl示例：路径必须替换为学校实际提供的文件
set library_path /actual/course/library.db
set_app_var target_library [list $library_path]`,
'Tcl是控制工具的软件脚本语言，不是Verilog。set给脚本变量赋值；$library_path取变量值；方括号[...]先执行里面的命令并使用返回值；list创建列表。set_app_var是DC用来设置应用变量的命令。不要把Tcl的方括号误认为Verilog位选。',
'正式脚本使用$env(STDCELL_DB)读取外部环境变量中的库路径，避免把某个人的机器路径写死。没有实际库时不应原样执行上面的示例路径，更不能把别的文件改名成.db来凑齐。'),
T('4 · 从文字到具体设计的几步',['步骤','作用','检查重点'],[
['analyze','读取并分析RTL源文件','语法、文件列表'],['elaborate counter','按顶层和参数建立具体设计','模块名、WIDTH'],['current_design counter','选择当前操作对象','报告针对谁'],['link','解析引用','模块/单元是否有定义'],['check_design','检查结构问题','重要警告是否解释']],
'仿真用4位实例并不会改写模块定义里的默认WIDTH=8。如果DC按默认参数展开counter，得到的是8位设计。测试和综合可以采用不同参数，但记录必须写清，不能拿4位回绕测试假装覆盖了所有8位边界。'),
S('5 · 模型文件的角色要分清',
'本课程DC读取学校提供的.db综合库；门级仿真需要相应的Verilog单元行为模型。两种文件服务于不同工具和问题，不能互相改扩展名来替代。模拟电路的器件模型也不是可以随意替换的标准单元库。',
'出现unresolved reference，先找到到底哪个实例引用了哪个名字，再查对应文件或库是否读入。把缺失模块当黑盒继续跑可能隐藏真实缺口；只有实验规格确实要求黑盒且提供必要模型时才这样处理。')],
Q('测试台指定WIDTH=4，为什么DC默认综合仍可能是8位？','参数覆盖只作用于测试台里的那个实例。DC以counter为顶层并使用其默认参数时会展开8位设计；需要显式核对各流程的顶层与参数。'));

attach('constraints',65,
'第4课把上升沿看作采样时刻，第8课理解寄存器更新。现在补上真实硬件的时间：数据经过门和连线不会瞬间到达，必须在接收寄存器要求的窗口内稳定。先理解这件事，再写约束。',[
S('1 · 从一个寄存器到下一个寄存器',
'想象寄存器A输出经过加法器进入寄存器B。A在一次上升沿之后才把新值送出，加法器计算和连线传播也花时间，B通常要在下一次上升沿采到结果。时钟周期就是这类路径可用预算的重要来源。',
'所谓路径，指数据从起点经过组合单元和连线到终点的传播关系。不是源文件里从第一行执行到最后一行，也不是看代码有多少行。一个短表达式可能形成很长的组合路径。'),
S('2 · 建立时间和保持时间约束的是采样窗口',
'建立时间（setup）要求接收端D在采样边沿之前已经稳定一小段时间。到得太晚，会侵占这个窗口。保持时间（hold）要求采样边沿之后D还保持稳定一小段时间；新数据到得太早，也可能破坏窗口。',
'因此“越快越好”不适用于所有检查。setup关心较慢路径是否赶得上，hold关心较快变化会不会过早到达。真实分析还考虑时钟到达差异、库模型、外部延时等。这里先建立窗口概念，不直接套复杂公式。'),
T('3 · 手算一个简化的建立预算',['项目','教学假设'],[
['时钟周期','10 ns'],['发射寄存器输出延迟','0.4 ns'],['组合逻辑与连线延迟','6.0 ns'],['接收建立时间','0.6 ns'],['预留不确定性','0.2 ns'],['数据到达时间','0.4+6.0=6.4 ns'],['允许到达时间','10-0.6-0.2=9.2 ns'],['建立裕量','9.2-6.4=2.8 ns']],
'本例假设时钟无偏斜、无其他例外，仅为解释。若组合与连线延迟变成9.0 ns，到达时间9.4 ns，裕量就是-0.2 ns。负值说明在这些条件下晚到0.2 ns。真实报告以工具所用完整模型为准。'),
C('4 · create_clock告诉工具目标，不产生硬件时钟',`# SDC/Tcl；只有核对时间单位为ns后才按此解释
create_clock -name core_clk -period 10 [get_ports clk]`,
'get_ports clk选中名为clk的设计端口；create_clock在该端口建立时钟分析对象，名字叫core_clk，周期10个时间单位。单位为ns时，对应100 MHz。把周期改成5 ns是提出200 MHz要求，不会自动让电路达到这个速度。',
'测试台always #5生成仿真输入波形；create_clock给分析工具规定时钟关系。一个安排实验，一个规定实现目标，二者不是同一用途，也不能互相替代。'),
S('5 · 为什么模块外面的时间也要告诉工具',
'输入端口的信号来自外部电路，可能在参考时钟之后一段时间才到本模块。set_input_delay描述这部分外部到达预算；set_output_delay为输出后面的外部接收路径规定相关预算。它们不是给RTL插入#延时，也不是直接指定模块内部某根线多慢。',
'max和min分别描述用于相应检查的较晚与较早情况，尤其不能只设置max就认为setup和hold都完整了。数值来自接口规格和时钟关系。本课模板只是已声明的练习假设，不能照抄成比赛板卡约束。'),
S('6 · 约束不是越少越容易成功',
'未约束路径可能没有受到你期望的时序检查。false_path把指定关系排除，multicycle改变检查的周期关系；这些命令改变问题定义，不会实际加速电路。不能为了让报告变绿就屏蔽失败路径。',
'先能解释哪条路径在什么时候被采样，才考虑时序例外。课程计数器使用同步复位，rst_n参与同步逻辑，不能仅因为端口名里有reset就随意排除。先保留真实失败，再分析是否应改设计、目标或有依据的约束。')],
Q('当10 ns周期的建立检查通过时，能否直接认定保持检查也通过？','不能。建立检查防止数据太晚，保持检查防止新变化太早；二者涉及不同路径延迟和采样窗口，必须分别分析。'));

attach('dc-run',60,
'第18课明确要实现什么，第19课提供可用元件，第20课给出时间预算。现在将这三项输入交给DC，再用报告回答“结构完整吗、检查覆盖了吗、目标满足吗”。',[
S('1 · 脚本顺序反映依赖关系',
'没有读入RTL就没有设计可展开，没有库就无法正确映射，没有时钟和接口约束就缺少性能目标。因此先读库与RTL、展开顶层并link、检查结构，再加载约束和检查覆盖，最后compile并写报告。',
'DC中的compile是综合优化命令，不是VCS生成仿真程序的那个流程。相同英文词在不同工具中承担不同工作。读脚本时先问它当前操作的是源文件、设计对象还是报告。'),
C('2 · 报告是文本文件，先确保目录存在',`# DC/Tcl片段；已完成库、设计、约束设置
file mkdir reports
compile
report_area > reports/area.rpt
report_timing -delay_type max > reports/setup.rpt
report_timing -delay_type min > reports/hold.rpt`,
'file mkdir创建目录，>把命令产生的报告保存到右侧文件。这样离开终端后仍可复核。max/min分别用于相应最大/最小延迟分析；本课重点查看建立与保持相关路径，不把它们混成一份数字。',
'不要只复制最后的面积和slack。先查看check_design、check_timing和关键警告：有没有缺失引用、意外锁存器、没有时钟、未约束端口或被异常删除的逻辑。输入条件不完整时，数字可能具有误导性。'),
T('3 · 先用教学路径学会读字段',['报告字段','例子','怎么读'],[
['Startpoint','state_reg/Q','数据从哪里发出'],['Endpoint','count_reg/D','数据在哪里被接收'],['Path group / clock','core_clk','按哪个时钟关系检查'],['data arrival time','10.1 ns','数据到达终点的时间'],['data required time','9.4 ns','最迟允许到达的时间（建立）'],['slack','-0.7 ns','晚到0.7 ns，当前条件下违例']],
'这是解释字段的示例，不是本项目实际DC运行结果。真实报告应读完整路径和单位，确认时钟、库条件与约束。建立关系可用required-arrival理解，保持分析不能机械照搬这个方向。'),
S('4 · 面积数字的含义取决于库',
'面积报告反映所选单元及库定义的面积模型。它不直接等于已经布线的芯片总面积，不包含你可以随意推测的封装或所有物理开销。FPGA资源报告中的LUT/FF数量也不应直接换算为ASIC面积。',
'比较两个RTL写法时保持参数、工具、库、工艺角与约束一致，并检查功能和时序。面积减少却时序失败，需要把取舍写出来，不能只报较小的数字。'),
S('5 · 交付一条可以复核的结论',
'一份好记录写清：什么源码版本、顶层参数、哪个库条件、哪套约束、有哪些警告、最差路径是什么、还有哪些检查未做。报告截图只截一行“slack正数”，无法说明其他路径是否被检查。',
'没有学校DC或目标库时，先完成示例报告的解释，实操保留待完成。课堂的正确目标是让你能读懂自己的真实结果，不是填入示例数值凑齐表格。')],
Q('综合退出成功，但check_timing提示没有时钟，为什么不能宣布时序达标？','分析缺少必要的时钟关系，预期的路径检查可能根本没有执行。先补齐有依据的约束和覆盖检查，再解释时序结果。'));

attach('post-synthesis',50,
'第21课已经拿到网表和报告。接下来不要只问“有没有生成文件”，而要问“生成的连接是否保持原功能，比较是否公平，还有什么未验证”。本课建立边界，不要求初学者立即搭建完整签核流程。',[
S('1 · RTL和门级网表描述同一目标的不同层次',
'RTL写加一、条件和寄存器更新；综合网表写目标单元实例及其连接。网表里可能不再保留你原来的表达式和信号名，但应在约定条件下实现相同功能。',
'若要仿真网表，仿真器必须知道网表里每种单元的行为。未解析某个单元名往往意味着缺少模型，不是已经证明设计逻辑错了。学校应提供与映射库对应的Verilog模型，.db不能直接充当它。'),
S('2 · 先核对边界，再谈比较',
'确认RTL和网表顶层名字、端口方向、位宽以及复位语义。参数化RTL经过综合后可能变成固定位宽网表，原测试台中的WIDTH参数覆盖不一定还能使用。',
'例如仿真是4位，而网表是默认8位，回绕周期不同并不一定是综合错误。应对同样规格和参数建立对应测试，或明确每份测试覆盖的参数范围。'),
T('3 · 几种检查各能说明什么',['检查','主要回答','不能据此直接宣布'],[
['RTL功能仿真','所测输入序列行为是否正确','所有输入都正确、物理时序满足'],['零延时门级仿真','单元连接在该模型下的行为','布线后速度达标'],['带延时门级仿真','指定延时回标与用例下的行为','全部条件下签核完成'],['形式等价','指定假设下两份设计的对应功能','板卡接口和物理实现全部可靠']],
'不同证据互相补充。完成入门项目不等于完成整颗芯片的验证和物理签核；知道缺口在哪里，比把所有检查都写“通过”更有价值。'),
S('4 · 延时回标为什么影响采样方式',
'SDF文件可以给门级模型补充时序延迟。原先在零延时RTL里上升沿后#1读取，也许到了带延时模型中数据还未稳定，或者触发了时序检查。不能沿用一个固定数字就认定测试方式正确。',
'先确认延时来源、匹配实例和回标范围，再按真实接口与采样窗口设计检查。初学阶段先理解这层差别，有正式环境和指导后再展开，不用为了做出漂亮波形随意延后检查来掩盖违例。'),
S('5 · 练习写一份公平比较说明',
'比较A、B两个版本时列出：功能规格相同吗、位宽相同吗、库与角落相同吗、周期与接口预算相同吗、检查范围相同吗？有任何一项不同，都先说明区别，再讨论面积与时序。',
'若A面积小但建立裕量为负，B面积大但达到目标，应根据项目要求判断取舍，而不是只选面积小的。保留两份报告与源码，后续才能解释为什么选择某个实现。')],
Q('用.db文件替换library_cells.v，为什么不能解决门级仿真的缺失单元问题？','它们提供给不同工具使用的模型。仿真器需要能解释这些单元行为的Verilog模型，综合库文件不能通过替换名称变成该模型。'));

attach('project',90,
'这是将前面的小步骤串起来的练习，不是再引入一套陌生工程。仍使用计数器：先冻结规格，再独立写RTL与测试，再保存实际运行结果；没有权限的工具步骤可以明确保留待完成。',[
S('1 · 先写规格，防止设计和测试各想各的',
'本项目是WIDTH位无符号二进制计数器，上升沿触发，低有效同步复位，复位优先于使能。使能时加一，否则保持；最大值后回到0。默认WIDTH=8，WIDTH必须为正整数。',
'先说出四位时的最大值15、八位时255，再写出复位、暂停与回绕时的预期。若你想做十进制计数器，应另立规格，不能一边写数到9回零，一边用模16的测试模型。'),
T('2 · 把项目拆成可以单独检查的交付',['步骤','产物','通过这一小步的条件'],[
['规格与手算','接口表、六项测试计划','每项有输入、边沿、预期'],['RTL','counter.v','能解释每个端口和优先级'],['测试台','tb_counter.sv','复位、计数、保持、回绕等逐拍检查'],['本地运行','命令、日志、VCD','本次编译运行成功，波形与预期一致'],['学校工具','VCS/DC真实记录','在可用授权环境实际执行'],['总结','README和待办','别人能知道怎么复现、哪些未验证']],
'每次只推进一个可检查的小步。不必一口气同时修复RTL、脚本、库和约束；哪一步没通过就先定位这一层。'),
S('3 · 先写自己的最小版本，再看参考包',
'先根据规格写RTL、手算几个关键边沿，再独立写测试台。确实卡住时，带着具体问题看第11—13课和下载包。参考文件能帮助核对，但直接复制无法检验你是否会从规格推导。',
'参考包采用4位实例加快回绕测试，综合默认8位。至少记录两个流程参数的不同；条件允许时再增加WIDTH=1和默认8位的功能测试，检验参数边界，而不是只测一个方便的配置。'),
S('4 · 一次失败也是有价值的交付',
'保存一个你能复现的错误：哪一拍、给了什么输入、预期多少、实际多少、根因是什么、修复后新增了什么检查。若暂时没有自然出现的错误，可在练习副本做明确标注的故障注入，不要编造真实故障经历。',
'修复后运行完整回归。最后一张PASS截图不能代替之前的分析，但一条有源码和向量的失败记录能证明你知道如何定位问题。'),
S('5 · README写给一周后的自己',
'说明目录、顶层、参数、工具版本、运行位置、命令、输出位置和测试范围。尤其写明用到的外部库及约束假设，避免下周忘了为什么这个路径有效、那个频率成立。',
'没有DC或库时，可以先交付本地功能验证与设计说明，将综合列为待完成。代码理解、功能仿真和学校工具实操分别记录；不要为了凑齐项目而冒充未运行的结果。')],
Q('如果下载包已经能PASS，这个项目还要求你独立做什么？','从规格解释或独立写出RTL，给出自己的测试计划和关键手算，实际运行并定位问题，说明工具、参数、覆盖范围及未完成项，而不只是启动参考脚本。'));

attach('acceptance',50,
'最后一课回到两个问题：你能解释为什么电路这样工作吗？你能拿出实际执行过的证据吗？课程打勾方便记录学习，但这两个问题要分别回答。',[
S('1 · 先口述一条完整因果链',
'选一个具体上升沿，说明之前的rst_n、en和count，然后推出更新后的count。再说明测试台何时驱动、何时采样、怎样得到expected。若中间某一步只能说“参考代码就是这样”，就回到对应课补这一小段，不必否定整段学习。',
'接着解释综合顶层为什么选counter、库提供什么、时钟约束表达什么、报告能证明什么。你不需要背完命令选项，但应知道每一步的输入、输出和意义。'),
T('2 · 用证据区分不同程度的完成',['你能做到的事','能支持的结论','还不能自动推断'],[
['解释复位/计数/保持的逐拍结果','理解这部分RTL行为','已运行工具'],['保存自己的RTL与测试台','完成实现与测试设计草稿','测试已经通过'],['实际自检日志和对应波形','本次配置与用例通过','所有输入、参数均正确'],['实际库/约束/网表/报告','完成有条件的综合检查','布局布线和芯片签核完成']],
'这不是压低你的成果，而是让成果表述准确。申请实习或介绍项目时，能清楚说明验证范围和剩余问题，比把所有事情都说“已经完成”更可信。'),
S('3 · 做一次小型复现',
'把源码、脚本、工具版本和说明交给有同等环境的同学，或者隔一天在干净工作目录自己重跑。观察对方是否需要猜你当时的路径、参数和库设置。缺少的信息补进README。',
'不要随项目传播密码、许可文件或无权分享的商业库。文档写清这些外部前提如何由学校提供即可。复现失败先分类：环境缺失、文件缺失、脚本错误还是设计行为不一致。'),
S('4 · 根据具体薄弱点返回，不按课号盲目重学',
'接线看不懂，回第5和11课；采样时刻不清楚，回第8和12课；不会写预期，回第13课；运行旧程序，回第14—17课；混淆库、约束和网表，回第18—22课。写下一个明确问题，再针对它复习。',
'一次只选一个能验证的小目标，例如独立增加暂停用例并看到它抓住故障。达成后再进入FIFO、流水线或图像项目。比赛推进和基础掌握可以互相促进，但项目可运行不会自动让所有概念变成熟练。'),
S('5 · 留下一份诚实的阶段复盘',
'写下最初的预期、出现的具体差异、定位过程、修复、增加的测试，以及尚未执行的工作。能解释一次错误通常比收集大量没有上下文的PASS截图更能帮助你继续学习。',
'已经完成的课程记录可以保留。补读讲解、重做旧练习或标记待复习都由你按实际理解决定，不需要为了新内容把旧成果全部清零。')],
Q('怎样描述“本地仿真已通过，但没有学校DC环境”的真实成果？','说明自己完成了哪些RTL、哪些参数和测试用例，提供本次编译、自检和波形证据；把DC综合、目标库和时序报告明确列为待完成，不能把网页示例当作实际结果。'));
})();
