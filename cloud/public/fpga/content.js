(function(root){
const Q=(q,options,correct,why)=>({q,options,correct,why});
const CONTENT={};
const sources={simulation:['AMD UG900 · 仿真','https://docs.amd.com/r/en-US/ug900-vivado-logic-simulation'],synthesis:['AMD UG901 · 综合','https://docs.amd.com/r/en-US/ug901-vivado-synthesis'],constraints:['AMD UG903 · 约束','https://docs.amd.com/r/en-US/ug903-vivado-using-constraints'],implementation:['AMD UG904 · 实现','https://docs.amd.com/r/en-US/ug904-vivado-implementation'],board:['AMD UG908 · 下载与调试','https://docs.amd.com/r/en-US/ug908-vivado-programming-debugging']};
const flow=[['RTL','定义输入、输出与状态；检查顶层和接口'],['仿真','测试台驱动输入；检查断言和第一次错误波形'],['综合','把 RTL 映射为逻辑资源；看网表、锁存器和警告'],['实现','布局布线连接真实资源；不是简单再编译一次'],['时序 / 资源','读实现后报告；检查约束覆盖、建立与保持、资源余量'],['Bitstream','为支持 .bit 的目标器件生成配置；其他器件配置格式以其文档为准'],['板级验证','下载到匹配器件，测量真实引脚行为，记录与仿真的差别']];
function add(id,data){CONTENT[id]={...data,completion:['我能用自己的话解释本课硬件行为','我已完成引导题与独立题，并对照预期自查','我已区分仿真/实操证据和仍待完成的部分']};}
add('01',{
 goals:['分清 FPGA、MCU 与 ASIC 的执行方式','把一条 assign 对应到组合电路'],
 concepts:[['重新配置的是电路关系','MCU 通常由固定处理器取指执行程序；FPGA 用配置数据确定可编程逻辑和连接，让多个硬件模块同时工作；ASIC 的逻辑实现则在制造时确定。FPGA 不意味着任意任务都比 MCU 快：并行性、时钟、接口和资源都限制了实现。你的 Verilog 仍在描述硬件，进入 FPGA 分支并不需要丢掉主线的组合/时序知识。'],['从真值表理解 LUT','LUT 是查找表，可以按输入组合给出预先配置的输出。a、b 两位输入有四种组合，与门的结果为 0、0、0、1。综合工具能用 FPGA 逻辑资源实现这张表。实际 LUT 大小随器件架构而异，不能假设每个与门都单独占用一整块 LUT；工具会合并和优化。']],
 hardware:'a 和 b 同时送入组合函数，y 随输入改变，没有等待时钟，也没有记住上一次值。把两个输入都连到开关只是板级实现的一种方式；本课先用测试台提供输入。',diagram:['a / b 输入','LUT：与逻辑','y 输出'],code:'rtl/and_gate.v',
 steps:['启动学校支持、且包含目标器件支持包的 Vivado，先认识 Flow Navigator 和 Sources 两个区域。还没有确定板卡时只观察界面，不随意选择一个器件去下载。','打开本课 and_gate.v，找到模块名、输入端口和输出端口。对照主线第 2 课的真值表，解释 assign 的并发含义。','读完整开发流程图；指出仿真检查功能，综合/实现建立硬件映射，板卡验证才观察真实电路。下一课再创建工程。'],
 expected:'能够把 00、01、10、11 对应到 y=0、0、0、1，并说明“工具打开了”不等于“板卡运行了”。本课不要求买板或生成配置文件。',wave:{heads:['输入 a,b','预期 y','解释'],rows:[['00','0','两者都不成立'],['01 / 10','0','只有一方成立'],['11','1','两者同时成立']]},
 mistakes:['把 Verilog 当作逐行执行的 C 程序。','看到 LUT 就认为 FPGA 内每个门都是物理独立的一块资源。'],debug:['先列真值表，再判断表达式。','检查是否存在寄存器；没有时钟的组合表达式不产生一拍延迟。'],
 guided:{task:'在 and_gate 中找出模块名、三个端口与连续赋值。写出四行真值表，并解释是否需要时钟。',steps:['先按 a,b=00/01/10/11 列行。','逐行套用“同时为真才为真”。','最后检查 RTL 是否包含保存状态的过程。'],hint:'y 只有在 11 时为 1；这里只有组合逻辑。'},
 independent:{task:'把功能改成“a 或 b 任一有效就输出 1”。写新模块，并说明 OR 和 XOR 在哪一组输入不同。',hint:'使用逐位或 |；对 11 单独检查，不要用 ^ 替代。'},
 quiz:[Q('FPGA 配置主要决定什么？',['可编程逻辑及连接','C 程序循环次数','芯片外壳尺寸'],0,'FPGA 配置描述硬件资源的功能和互连。'),Q('assign y=a&b 中没有状态存储，输出依赖？',['上一次 y','当前 a、b','已运行多少行代码'],1,'它是组合函数。')],source:'synthesis'});
add('02',{
 goals:['建一个干净的 RTL 工程','区分设计顶层与测试顶层'],
 concepts:[['工程不是一份 .v 文件','Vivado 工程记录器件型号、设计源、仿真源、约束和运行设置。顶层是电路向外暴露接口的边界。一个项目可以有很多模块，但本次实现只有选定的设计顶层。选择完整 Part 时，封装与速度等级也属于目标条件。'],['把源文件放对集合','Design Sources 放要实现的 RTL；Simulation Sources 放 testbench。测试台里的 #5 和 $finish 只是仿真实验控制，不能变成板载晶振或硬件停止按钮。课程先用 and_gate 作为设计顶层，tb_gate 作为仿真顶层。']],
 hardware:'输入输出端口最后可能连接封装引脚，内部模块端口只是层级之间的线。先确定顶层接口，再做引脚分配，避免把测试台当成待下载的电路。',diagram:['设计源 and_gate','设计顶层端口','后续 XDC / 引脚'],code:'rtl/and_gate.v',
 steps:['在 Vivado 欢迎页选择 Create Project，给工程起名 fpga_gate，选择可写的新目录，项目类型选 RTL Project。暂不添加 IP 或 Block Design。','在 Add Sources 中加入 and_gate.v，语言选 Verilog。在 Default Part / Boards 处，按老师指定板卡核对器件全名；未确定时可使用明确记录的教学器件做仿真，不能据此下载到未知板。','完成后在 Sources 的 Design Sources 下检查 and_gate，右键 Set as Top（若已自动识别则核对即可）。在 Project Summary 记录目标 part。','通过 Add Sources → Add or Create Simulation Sources 加入 tb_gate.sv，设 tb_gate 为仿真顶层；查看 Compile Order，确认测试台能找到 and_gate。'],
 expected:'Sources 中设计与仿真分组清楚，设计顶层为 and_gate，仿真顶层为 tb_gate；工程记录的器件能说明来源。',
 mistakes:['把 tb_gate 设为设计顶层。','按名字相似选错芯片封装。','拷贝文件后忘记工程引用的是旧目录。'],debug:['查看文件的完整路径和 File Type。','检查 module 名与 Set as Top 一致。','确保本次编辑的是 Sources 指向的文件。'],
 guided:{task:'按步骤创建工程，记录工程名、设计顶层、仿真顶层、目标器件及其来源。尚未确定板卡则明确写“教学目标，未上板”。',steps:['分别找到两组 Sources。','核对模块声明，不用文件名代替模块名。','把四项配置写到回答中。'],hint:'设计顶层 and_gate；测试顶层 tb_gate。目标 part 由实际板卡/老师资料确认。'},
 independent:{task:'创建 register8 的工程，写出其四类输入和输出位宽。说明如果添加了两个不同的 testbench，你如何选择本次仿真顶层。',hint:'时钟、复位、使能和 8 位数据是不同接口；仿真集合只选定一个顶层。'},
 quiz:[Q('测试台应该放在哪个源集合？',['Constraints','Simulation Sources','Board Files'],1,'测试台组织仿真，不作为设计电路综合。'),Q('目标器件为什么要核对封装？',['决定可用封装引脚及资源条件','只影响界面颜色','不影响任何结果'],0,'引脚映射与真实器件封装必须一致。')],source:'simulation'});
add('03',{
 goals:['运行行为仿真并读逐拍结果','在测试台中自动判断错误'],
 concepts:[['时钟来自测试台','行为仿真用事件和时间模型执行 RTL，并不需要真实板卡。tb_register 的 always #5 每 5 ns 翻转 clk，因此周期是 10 ns。testbench 在下降沿改输入，在上升沿后 #1 检查输出，是为了避开寄存器非阻塞更新的采样竞态。'],['验证不是看见一条波形','先预测：en=1 时 q 采到 d；en=0 时 d 改变但 q 保持；同步复位必须等上升沿。再执行仿真，找第一次偏离预期的边沿。$fatal 让错误明确失败；$finish 结束正常实验。仿真 PASS 的范围只限于这次用例。']],
 hardware:'register8 对应 8 位状态和更新选择：复位清零优先，其次使能采样，否则保持。时钟高电平期间并不是一直执行赋值。',diagram:['d / en / rst_n','上升沿采样','8 位寄存器 q'],code:'rtl/register8.v',tb:'tb/tb_register.sv',
 steps:['把 register8.v 加入 Design Sources，tb_register.sv 加入 Simulation Sources；分别核对两个顶层。','Flow Navigator → Simulation → Run Simulation → Run Behavioral Simulation。若编译失败先读 Messages 中首个错误，不继续分析旧波形。','在 Scope / Objects 中把 clk、rst_n、en、d、q 加入波形，使用 Run All 或足够的运行时间。观察 q 的变化只发生在有效上升沿之后。','查看仿真日志中的 PASS register；将 RTL 副本的 en 判断故意去掉，再运行，预期 hold 检查报错，随后恢复。'],
 expected:'依次看到 q=A5、保持 A5、复位为 00；日志 PASS register。错误副本必须触发 hold 失败。',wave:{heads:['边沿前 rst_n,en,d','边沿后 q','检查目的'],rows:[['1,1,A5','A5','正常采样'],['1,0,3C','A5','使能关闭保持'],['0,0,3C','00','同步复位']]},
 mistakes:['在同一个上升沿驱动 d 并立即读 q。','仿真没有重新启动，看到的还是旧设计状态。'],debug:['复位至少跨过一个有效上升沿。','检查 Testbench 的 timescale 和 $finish。','每次改源码后重新编译/重启仿真。'],
 guided:{task:'运行本课测试台，写出三个检查点的预期与实际。若还未装工具先手算，证据标为待运行。',steps:['先填预期表。','运行正确设计并保存日志。','注入一次保持错误，确认自检能失败。'],hint:'保持检查最容易暴露 en 被忽略；不要用 q 自己作为预期。'},
 independent:{task:'增加“en=1 同时 rst_n=0”用例，验证复位优先。写出激励与比较代码。',hint:'在下降沿同时设置，下一上升沿后 #1 比较 q 是否全零。'},
 quiz:[Q('always #5 翻转时钟的完整周期是？',['5 ns','10 ns','20 ns'],1,'本测试台单位 ns，两次翻转构成一个周期。'),Q('#1 检查延迟应该写在哪里？',['所有 FPGA RTL 中','测试台中','XDC 引脚属性中'],1,'此处是仿真调度安排，不是硬件延迟实现。')],source:'simulation'});
add('04',{
 goals:['理解综合输出是什么','检查锁存器、被优化信号与顶层'],
 concepts:[['综合解释硬件行为','综合把可综合 RTL 展开、优化并映射为逻辑资源。在 FPGA 中目标是器件支持的逻辑单元，而主线 DC 目标通常是标准单元库。两者都需要明确设计顶层与约束，但工具和资源模型不同。'],['结果未必逐行对应','连续赋值可能被合并，常量或未使用的输出可能让逻辑被删除。reg 类型不保证一个触发器；always @(*) 不完整赋值可能推断锁存器。先知道预期结构，再看工具结果，才能分辨正常优化和设计接线错误。']],
 hardware:'register8 的上升沿过程表达状态；若所有 8 位输出都使用，预期存在相应状态资源。具体控制端和选择逻辑映射以器件网表为准，不要求死记 LUT 数。',diagram:['register8 RTL','展开 / 优化','FF 与输入选择逻辑'],code:'rtl/register8.v',
 steps:['确认 register8 是设计顶层，testbench 在 Simulation Sources。先完成一次行为仿真。','Flow Navigator → Run Synthesis，等待运行成功，再选择 Open Synthesized Design。','查看 Messages 的首个 Critical Warning/Warning，搜索 latch、trimmed、unconnected；逐项解释，不把所有 warning 都忽略。','打开 Report Utilization 和 Schematic，查找 q 的状态资源；记录 top、part、参数、警告与资源分类。此时还没有完成布局布线。'],
 expected:'得到综合后的逻辑网表和资源报告，能说明是否符合一个 8 位使能寄存器的行为。综合成功不能替代功能仿真。',
 mistakes:['把综合成功当作所有功能正确。','输出没接导致逻辑消失，却以为工具坏了。'],debug:['先核对 top 和输出连接。','定位每条警告对应源码。','检查是否有意外锁存器或多重驱动。'],
 guided:{task:'综合 register8，记录 FF、LUT 类别及警告。没有实际报告就不要填数字，先画出预期结构。',steps:['保存仿真结果。','综合并打开网表。','用端口 q 定位资源，注明实际或预期。'],hint:'报告中的数量依赖目标器件和优化；理论预期与实测报告应分开写。'},
 independent:{task:'把 en 改为常量 1 的独立顶层连接，重新综合。对比控制逻辑是否变化，保持相同 part 和约束。',hint:'只改一个条件；不要假定 FF 数也一定减少。'},
 quiz:[Q('综合后的结果已经是完成布线的板卡电路吗？',['不是，还需要实现','是','只差截图'],0,'综合建立逻辑资源映射，实现才进一步布局布线。'),Q('未使用输出相关逻辑被删除可能是？',['正常优化','仿真时间太短','USB 接错'],0,'先检查信号是否能影响可观察输出。')],source:'synthesis'});
add('05',{
 goals:['区分 RTL 原理图与映射后的原理图','从寄存器输入反查逻辑'],
 concepts:[['两张图回答不同问题','Elaborated Design 的 RTL Schematic 更接近展开后的结构与运算关系；Synthesized Design 的 Schematic 反映综合优化后的实现。画面中的符号是工具对网表的呈现，不是芯片版图，更不是 PCB 走线。'],['沿信号方向追踪','从 q 反向找到驱动它的寄存器，再追 D 端选择条件和上游输入。对一个多路选择器，确认 sel 如何决定输出；对寄存器，确认时钟和复位在哪一条路径上。层级展开只是换一种视图，不会增加实际电路。']],
 hardware:'两个输入进入与逻辑后驱动 y；更复杂时多路选择和寄存器连接构成数据通路。原理图帮助你检验代码含义，但不能替代完整测试。',diagram:['a / b','组合逻辑','y / 寄存器 D'],code:'rtl/and_gate.v',
 steps:['在 RTL Analysis 下 Open Elaborated Design，进入 Schematic，选择 y 端口观察上游。','记下看到的逻辑关系，再运行综合并打开 Synthesized Design 的 Schematic。','切换到 register8，找到 clk、q 与输入选择；尝试展开一个层级，再返回上层，留意哪些是真实连接。','用代码和图各解释一次 en=0 时数据为何保持，不只记录工具截图。'],expected:'能够区分“RTL 结构理解”与“已映射资源”，并从输出追溯到输入。',
 mistakes:['把 RTL Schematic 里的加号框当成最终物理加法器布局。','只看图形数量，不核对位宽和驱动。'],debug:['从输出倒推，确认线宽。','双击/展开层级后检查当前路径。','若端口不见了，检查优化和常量传播。'],
 guided:{task:'用 and_gate 的源码与原理图各解释 y 的驱动关系。记录 RTL 图与综合图可能为何不同。',steps:['先口述真值关系。','在图中找到 a、b 和 y。','说明优化不要求逐行保留。'],hint:'等价行为可以有不同结构呈现。'},independent:{task:'画出一个 4 位 2:1 MUX 后接 4 位寄存器的模块分解，写出关键 assign 与 always，说明图中组合和存储分别在哪里。',hint:'sel 选择 a/b 到 d，q 只在时钟边沿采 d。'},quiz:[Q('RTL 原理图是芯片版图吗？',['是','不是','只有有寄存器时才是'],1,'它是逻辑结构视图。'),Q('追踪 q 的原因链，应该先看？',['驱动 q 的寄存器及其输入','图片颜色','文件夹排序'],0,'信号驱动关系才解释输出如何产生。')],source:'synthesis'});
add('06',{
 goals:['认识 LUT、FF、BRAM、DSP 的用途','避免由语法直接猜资源'],
 concepts:[['计算与保存各有资源','LUT 常实现组合函数，FF 保存单个位状态；BRAM 是块存储资源，适合一定规模与端口/时序要求的存储；DSP 资源提供乘加等运算能力。它们是硬件预算，不是四种软件变量类型。具体数量与特性以目标器件文档为准。'],['写了数组不一定是 BRAM','很小的只读表、异步读结构或不合适的复位方式，可能映射成 LUT 或寄存器。乘法也不保证一定用 DSP，位宽、约束、综合设置都会影响映射。先表达正确功能，再看利用率报告验证资源推断。']],hardware:'本课四项小 ROM 用两位地址选八位数据；容量很小且为异步读，通常不需要一个大块 BRAM。它仍是硬件表，不会逐行读取 case。',diagram:['2 位地址','查表选择','8 位数据'],code:'rtl/small_rom.v',
 steps:['把 small_rom 设为设计顶层，使用相同器件运行综合。','查看 Utilization 中 LUT、FF、Block RAM 项；写下实际结果，不预填 BRAM=1。','另建 multiply8 的综合运行，比较 DSP/FF/LUT；只作为资源观察，不以不同 part 的数字互相比较。','打开报告中的层级统计，找到资源属于哪个模块，记录位宽与目标器件。'],expected:'能把每类资源与用途对应，并解释小 ROM 为什么可能没有使用 BRAM。',mistakes:['reg 数组一定是块 RAM。','所有乘法都消耗一个 DSP。'],debug:['核对存储深度、同步/异步读写。','检查输出是否被优化。','比较时固定器件和参数。'],guided:{task:'综合 small_rom，做一张资源用途表，再填实际报告；不能运行时只填用途，不猜数量。',steps:['列出 LUT/FF/BRAM/DSP。','解释本 ROM 的读方式。','查实际映射。'],hint:'小容量异步查表一般更适合 LUT 类实现。'},independent:{task:'解释 multiply8 中 y 为什么是 16 位。列出要用哪份报告确认乘法是否映射到 DSP。',hint:'8 位无符号最大值 255，255×255 需要 16 位；查看综合资源报告。'},quiz:[Q('FF 主要负责？',['保存状态','连接 USB','存储源代码文件'],0,'触发器提供状态位。'),Q('很小的异步读 ROM 一定使用 BRAM 吗？',['一定','不一定','只能由 MCU 执行'],1,'存储结构与规模影响资源推断。')],source:'synthesis'});
add('07',{
 goals:['区分物理引脚约束与时序约束','按实际晶振写 create_clock'],
 concepts:[['XDC 是设计与外部条件的约定','RTL 写功能，XDC 描述实现所需的物理与时间条件。PACKAGE_PIN 关联顶层端口和封装引脚；IOSTANDARD 描述 I/O 电气标准；create_clock 告诉工具分析时钟周期。XDC 不会在板上生成一个时钟，也不会让错误接线变正确。'],['不要把示例值当板卡事实','板卡未确定时，可以计算示例周期，但不能给出可下载的引脚表。实际时钟若为 100 MHz，周期为 10 ns；50 MHz 则为 20 ns。必须从原理图/主 XDC 核对真实值。错误时钟预算会让时序结论失去意义。']],hardware:'与门例子没有寄存器时不需要虚构时钟端口；有 register8 时，外部时钟进入 clk，XDC 约束的是这条实际信号。',diagram:['实际板卡资料','顶层端口 / 时钟','XDC 约束实现'],code:'rtl/register8.v',
 steps:['从板卡厂商资料核对型号、完整 part、晶振频率、引脚和 I/O 电压，把来源记录在本课板卡信息中。','Add Sources → Add or Create Constraints，创建 board.xdc；先写实际顶层端口，使用 get_ports 选择对象。','若已核对真实晶振为 100 MHz，才写 create_clock -name board_clk -period 10 [get_ports clk]；其它频率按 1000/MHz 计算。','在 Open Synthesized Design 后检查约束对象是否非空；实现后查看 Report Timing Summary 的时钟和未约束路径。下载的 guarded 模板会主动停止，需按资料替换，不能直接执行。'],expected:'得到有来源的时钟周期与物理约束草稿。没有确定板卡时，完成概念练习，实际 XDC 验证标为待完成。',mistakes:['复制别人的引脚号。','把 set_input_delay 当任意补救数值。','没有时钟约束却宣布时序通过。'],debug:['逐个核对 get_ports 是否匹配顶层名字。','检查时钟单位和频率。','不要随意 false_path 掩盖未满足的路径；异步输入处理需有明确对象与意图。'],guided:{task:'已知教学时钟 50 MHz，写 create_clock 并解释它在分析中做什么。区分教学假设与真实板卡值。',steps:['计算周期。','选 clk 端口。','注明只是给定题目条件。'],hint:'周期 20 ns；约束告诉工具目标，不是生成晶振。'},independent:{task:'拿到实际板卡手册后，列出 clk、led 的引脚/电压/极性/来源。未拿到时列出你需要向老师确认的字段，不编造。',hint:'封装引脚与开发板排针编号可能是两种编号。'},quiz:[Q('100 MHz 时钟周期为？',['100 ns','10 ns','1 ns'],1,'周期 ns=1000/频率 MHz。'),Q('create_clock 会产生物理晶振吗？',['不会','会','只在 FPGA 中会'],0,'它定义时序分析条件。')],source:'constraints'});
add('08',{
 goals:['建立端口到实际引脚的对应关系','理解 I/O 标准与板卡电压'],
 concepts:[['一个名字跨越三层','RTL 中的 led 是逻辑端口，芯片封装的引脚有自己的编号，开发板上 LED 或排针也有自己的标号。必须沿原理图把这三层连起来。GPIO 电压要匹配所在 Bank 的供电与外部器件，不能只为了消除工具报错选一个标准。'],['极性决定亮灭解释','有些 LED 高电平亮，有些低电平亮。内部逻辑可以约定 led_on=1 表示亮，在顶层再根据板卡取反。按键同理先确认按下时高/低，后续同步与消抖处理的是约定好的有效电平。']],hardware:'assign y=a&b 是逻辑关系；PACKAGE_PIN 指定 a、b、y 到哪几个封装管脚。这是逻辑功能和物理连接的分工。',diagram:['逻辑端口','封装引脚','板上开关 / LED'],code:'rtl/and_gate.v',
 steps:['打开厂商原理图或主 XDC，找到 LED 和开关网络，记录封装管脚而非仅记录丝印名。','在 XDC 为每个实际顶层 I/O 设置正确 PACKAGE_PIN 和 IOSTANDARD，向量端口用花括号保护名称，例如 get_ports {led[0]}。','在 I/O Planning / Package 视图核对端口与 Bank；不同版本面板位置可能不同，核心是检查端口对象和属性。','运行实现前 DRC，重点处理未约束 I/O 和未指定电气标准；不降低 UCIO-1/NSTD-1 严重性来硬生成 bitstream。'],expected:'形成逐端口可追溯的映射表，明确 LED 极性，工具约束对象匹配实际 top。',mistakes:['把排针编号直接填成 PACKAGE_PIN。','把 5V 外设直接当作 FPGA 可接受的输入。'],debug:['先断电核对接线与资料。','核对 Bank 电压和引脚用途。','从 Messages 定位具体端口，不全局关闭 DRC。'],guided:{task:'设计一个 pin mapping 表，包含 RTL 名、封装脚、板上网络、IOSTANDARD、有效电平、文档页码。没有板卡资料的项写待确认。',steps:['列顶层所有 I/O。','逐项找硬件资料。','检查命名与 XDC 一致。'],hint:'这张表是上板前的证据，不能只填“默认”。'},independent:{task:'假设资料明确 LED 为低有效，内部 led_on=1 代表亮，写顶层极性转换。解释为什么不改内部状态机语义。',hint:'assign led_pin=~led_on；内部“亮”的语义保持一致。'},quiz:[Q('PACKAGE_PIN 应填写？',['已核对的芯片封装引脚','随意排针编号','模块名'],0,'封装脚须从板卡资料对应出来。'),Q('遇到未约束 I/O 的 DRC 应？',['降低严重性绕过','补齐并核对实际约束','删除所有端口'],1,'DRC 保护实现与电气条件，不能拿绕过去替代修复。')],source:'constraints'});
add('09',{
 goals:['把按钮变为可靠的单次事件','按顺序完成第一次 LED 板级实验'],
 concepts:[['同步和消抖解决两个问题','按钮相对 FPGA 时钟是异步输入，用串联寄存器降低亚稳态传播风险；机械接点会在一次按下时反复跳变，消抖要求输入保持足够长时间才接受新电平。同步不等于消抖，消抖也不是只延迟一拍。'],['持续按住与一次脉冲不同','消抖后的 pressed 是稳定电平，rise=pressed & ~previous 是一拍的上升事件。LED 只在 rise 时取反，长按不会每拍翻转。参数 STABLE 按实际时钟和消抖时间决定；仿真可以用小值，但不能把仿真小值直接忘在板卡工程。']],hardware:'按钮经过两级同步、稳定计数再边沿检测，得到 press_pulse 驱动 LED 寄存器。物理复位也应正确同步释放；示例 rst_n 是已经满足时钟关系的接口，不能直接把未经处理的按键当作它。',diagram:['异步按钮','同步 + 消抖','一拍 rise','LED 寄存器'],code:'rtl/button_filter.v',additional:'rtl/led_toggle.v',
 steps:['先独立仿真 button_filter，缩小 STABLE，构造抖动、短脉冲、持续按住和释放；数出 rise 脉冲个数。','在顶层实例化 button_filter 与 led_toggle，把 rise 接 press_pulse；根据实际时钟恢复 STABLE，并处理复位和 LED 极性。','添加核对过的 XDC，依次 Run Synthesis、Run Implementation，打开实现设计检查时序与 DRC。','对支持 .bit 的目标器件 Generate Bitstream，Hardware Manager → Open Target → Auto Connect；核对检测到的器件后 Program Device，选择本次生成文件。','观察一次按下翻转一次、长按不反复翻转；记录板卡、时钟、参数、文件时间与实际现象。无板卡则保留为待上板，不勾实证。'],expected:'仿真中短抖动不触发，稳定按下产生一次 rise；板卡上一按切换亮灭。bitstream 下载完成只说明配置传输完成，仍需观察功能。',wave:{heads:['按钮情形','pressed','rise'],rows:[['短暂 0/1 抖动','保持旧值','0'],['持续为 1 达 STABLE 拍','变 1','1 拍'],['继续长按','保持 1','0'],['稳定释放','变 0','0']]},mistakes:['把原始按钮直接作为时钟。','只用两级同步就声称已经消抖。','用不同版本 bitstream 验证当前源码。'],debug:['先用固定 LED 电平验证极性与引脚。','再测试时钟心跳。','最后加入按钮同步/消抖和状态。'],guided:{task:'手算 STABLE=4 的稳定采样序列，再仿真一段抖动和长按。写出预期 rise 次数。',steps:['区分原始按钮、同步值、稳定值。','计数连续不同于旧稳定值的拍数。','rise 应只持续一拍。'],hint:'两级同步增加延迟，但持续长按最终只产生一次上升事件。'},independent:{task:'将按钮驱动 LED 扩成“按一次亮、再按一次灭”，独立写顶层连线与测试计划，实际上板结果单独记录。',hint:'不要对 press_pulse 写 always @(posedge press_pulse)；保持同一个 clk。'},quiz:[Q('两级同步器能消除机械抖动吗？',['能','不能，还需要消抖','只在 100MHz 能'],1,'亚稳态传播风险与机械重复跳变是两类问题。'),Q('长按按钮只翻转一次，应该用？',['原始按钮当时钟','去抖后的上升脉冲作使能','每拍取反'],1,'一拍事件让寄存器只更新一次。')],source:'board'});
add('10',{
 goals:['由主线计数器生成慢速使能','区分 clock enable 与新时钟'],
 concepts:[['让动作慢下来，不必造新时钟','LED 的可见变化比 FPGA 主时钟慢很多。计数到 DIV-1 时发一个周期的 tick，所有状态仍在 clk 上升沿更新，只在 tick=1 时动作。这样是一个时钟域，避免初学者随手把逻辑输出当新时钟带来的布线与约束问题。'],['把时间换算成计数','若实际时钟为 100 MHz，DIV=100000000 则每秒一个 tick。若每次 tick 翻转 LED，完整亮灭周期是 2 秒而非 1 秒。计数从 0 到 DIV-1 一共 DIV 拍，比较 DIV 会多一拍。这里不讲复杂时钟生成；真正新频率的时钟应按器件时钟资源规范设计。']],hardware:'计数器、相等比较器与一拍脉冲寄存器形成使能发生器。tick 是控制条件，不接其他 always 的时钟敏感列表。',diagram:['clk','计数 / 比较 DIV-1','一拍 tick','同一 clk 下的 LED 使能'],code:'rtl/tick_enable.v',
 steps:['把 tick_enable 加到工程，仿真实例改 DIV=4，驱动复位，观察连续多个周期。','在波形同时显示 count、tick 和下游 LED，注意非阻塞赋值下，下游读取的是边沿前的 tick。','综合时恢复根据真实板卡频率计算的 DIV；所有时序过程仍使用 posedge clk。','在时钟报告核对只存在有意的时钟，检查 tick 没有被误接为 clock。'],expected:'tick 每 DIV 拍有效一拍；下游看到 tick 后更新，初始相位按采样规则解释。',wave:{heads:['DIV=4，复位后边沿','count','tick'],rows:[['1','1','0'],['2','2','0'],['3','3','0'],['4','0','1'],['5','1','0']]},mistakes:['写 posedge tick 产生隐含新时钟。','把时钟周期与 LED 完整闪烁周期混淆。'],debug:['先用小参数手算周期。','核对 counter 位宽至少容纳 DIV-1。','根据真实晶振恢复硬件参数。'],guided:{task:'时钟 50 MHz，希望每 0.25 秒移动一次灯，算 DIV，并写明完整循环四个灯所需时间。',steps:['频率乘时间得到拍数。','四次移动构成四灯一圈。','记录仿真缩小参数。'],hint:'DIV=12500000；四次移动需要 1 秒。'},independent:{task:'用 tick 使能驱动一个可暂停计数器。解释 en=0 时 tick 发生器是否仍运行，以及你选择的恢复相位。',hint:'可保持 tick 自由运行，只让下游暂停；规格需明确恢复时是否从半个周期继续。'},quiz:[Q('0 数到 DIV-1 一共有几拍？',['DIV-1','DIV','DIV+1'],1,'包括零，共 DIV 个状态。'),Q('本分支推荐下游如何响应 tick？',['posedge tick','posedge clk 内 if(tick)','延时 #DIV'],1,'tick 是同一时钟下的使能。')],source:'synthesis'});
add('11',{
 goals:['把计数器、使能和状态连成流水灯','验证暂停与回绕'],
 concepts:[['灯的位置也是状态','四位 one-hot 表示位置：0001、0010、0100、1000。每个 tick 进行一次循环移位。复位必须给出合法状态 0001；若复位为 0000，只做循环移位会永远全灭。'],['暂停是保存，不是停止时钟','en=0 时寄存器保持，时钟仍正常运行；en=1 且 tick=1 才移动。按钮应先经过同步消抖成为控制事件，再切换运行状态。状态机管理模式，计数器管理时间，两者职责不同。']],hardware:'tick_enable 提供时间尺度，running_led 保存位置，按钮控制 en。它们并发工作，通过一拍使能和寄存器信号连接。',diagram:['按钮事件 → en','tick_enable','位置寄存器','4 路 LED'],code:'rtl/running_led.v',
 steps:['把 running_led 与 tick_enable 加入设计，先用小 DIV 仿真，验证至少两圈。','在 tick 到来前设置 en=0，观察灯位置保持；恢复 en 后按规格继续。','再接入去抖按钮控制 run 状态，检查一次事件只改变一次模式。','实际板卡四个 LED 分别约束，完成综合、实现、时序/DRC、配置和观察。记录每个灯的位序是否与原理图一致。'],expected:'有效移动为 0001→0010→0100→1000→0001；暂停时不动。U1 FPGA 阶段验收将在本课完成后开放。',wave:{heads:['动作','LED 状态'],rows:[['复位','0001'],['第1次有效 tick','0010'],['暂停期间 tick','0010'],['恢复后有效 tick','0100']]},mistakes:['复位为全零导致永远无灯。','把实际 LED 顺序与总线位序混为一谈。'],debug:['先验证 one-hot 逻辑…22866 tokens truncated…}
 if(route.startsWith('ultra-image-')){renderUltra(route.slice(12));common();return;}
 const m=C.modules.find(x=>route==='edge-'+x.id),d=D[m.id],v=s.modules[m.id]||={state:'NOT STARTED',draft:'',evidence:'',reviewed:false};
 host.innerHTML=header('COMPETITION '+m.id+' · '+m.phase,m.title,d.goal)+baseLink(m)+d.sections.map(([h,t])=>`<section class="reading-block"><h2>${e(h)}</h2><p>${e(t)}</p></section>`).join('')+flow(d.diagram)+codePanel(d.code)+(d.demo?`<section id="project-demo" class="lab-card"></section>`:'')+panel('对照例子',`<p>${e(d.example)}</p>`)+(d.steps?panel('从接口到独立验收',`<ol>${d.steps.map(x=>`<li>${e(x)}</li>`).join('')}</ol>`):'')+panel('常见错误与调试',list(d.errors)+`<h3>先查什么</h3>`+list(d.debug))+panel('独立练习',`<p>${e(d.exercise)}</p>${field('project-draft','代码、手算、时序图或调试记录',v.draft)}<p class="completion-note">代码片段须按题目补齐模块；网页不执行Python、RTL、Efinity或板卡下载。</p>`)+panel('Checkpoint 与项目状态',list(d.checkpoint)+field('project-evidence','实际验证证据 / 文件位置 / 尚未完成部分',v.evidence,3000)+`<label class="selfcheck"><input id="project-reviewed" type="checkbox" ${v.reviewed?'checked':''}>我已按本课检查点自查，并注明未运行的部分</label>`+select('project-state',v.state,C.states,'项目模块状态')+`<p class="completion-note">这里的COMPLETED表示模块学习与交付记录完成，不自动完成基础课、技能或项目里程碑。</p>`)+(d.downloads?panel('Python Golden 与比较工具',`<div class="project-links">${['golden.py','compare.py','README.md'].map(f=>`<a href="/project/kit/${f}" download>下载 ${f} ↓</a>`).join('')}</div><p>在本地运行；自带小图无需额外库，自定义PNG/JPEG需Pillow。</p>`):'')+panel('下一步',`<div class="button-row">${link('competition','项目总览')}${+m.id<21?link('edge-'+String(+m.id+1).padStart(2,'0'),'下一模块'):link('milestones','查看里程碑')}${link('project-journal','记录本次工作')}</div>`)+(d.sources?panel('官方工具资料',d.sources.map(([t,url])=>`<p><a href="${e(url)}" target="_blank" rel="noopener">${e(t)} ↗</a></p>`).join('')):'');
 $('#project-draft').oninput=ev=>{v.draft=ev.target.value;v.reviewed=false;$('#project-reviewed').checked=false;if(v.state==='COMPLETED'){v.state='VERIFYING';$('#project-state').value=v.state;}persist();};$('#project-evidence').oninput=ev=>{v.evidence=ev.target.value;persist();};$('#project-reviewed').onchange=ev=>{v.reviewed=ev.target.checked;if(!v.reviewed&&v.state==='COMPLETED'){v.state='VERIFYING';$('#project-state').value=v.state;}save();};$('#project-state').onchange=ev=>{const next=ev.target.value;if(next==='COMPLETED'&&(!v.reviewed||v.draft.trim().length<20||v.evidence.trim().length<20)){ev.target.value=v.state;toast('请先补齐解答、证据（各至少20字）和自查记录');return;}v.state=next;save();};if(d.demo)renderDemo(d.demo);common();
 function renderDemo(kind){
  const demo=$('#project-demo');
  if(kind==='sobel'){
   demo.innerHTML='<h2>3×3手算实验台</h2><p>JavaScript数学演示，不执行RTL。修改像素或阈值，观察signed梯度与L1。</p>'+`<div class="pixel-grid">${Array.from({length:9},(_,i)=>`<label>P${Math.floor(i/3)+1}${i%3+1}<input type="number" min="0" max="255" step="1" data-pixel="${i}" value="${(i+1)*10}"></label>`).join('')}</div><label class="fpga-field">阈值<input id="sobel-threshold" class="lab-input" type="number" min="0" max="2047" value="300"></label><p id="sobel-result" class="equation" role="status"></p>`;
   const update=()=>{const nodes=Array.from(demo.querySelectorAll('[data-pixel]')),values=nodes.map(n=>Number(n.value)),t=Number($('#sobel-threshold').value);if(nodes.some(n=>n.value==='')||values.some(x=>!Number.isInteger(x)||x<0||x>255)||!Number.isInteger(t)||t<0||t>2047){$('#sobel-result').textContent='像素填0..255整数，阈值填0..2047整数。';return;}const r=M.sobel(values,t);$('#sobel-result').textContent=`Gx=${r.gx} · Gy=${r.gy} · L1=${r.mag} · edge=${r.edge}`;};demo.querySelectorAll('input').forEach(el=>el.oninput=update);update();
  }else{let index=0;demo.innerHTML='<h2>5×5像素流 · 逐个接受</h2><p>这是理想索引演示，真实同步RAM还需延迟配对。像素值为y×5+x。气泡不接受新像素。</p><div class="button-row"><button id="window-next" class="lab-button">接受下一个像素</button><button id="window-bubble" class="lab-button">插入气泡</button><button id="window-reset" class="lab-button">重置演示</button></div><p id="window-info" role="status"></p><div id="window-pixels" class="pixel-grid"></div>';const update=note=>{const w=M.windowAt(5,5,index);$('#window-info').textContent=`${note||''} 当前输入(${w.col},${w.row})=${index}；${w.valid?'WINDOW VALID，中心('+w.center.join(',')+')':'WINDOW INVALID：行/列尚未填满'}`;$('#window-pixels').innerHTML=w.pixels.map(x=>`<div class="pixel-cell">${x===null?'—':x}</div>`).join('');$('#window-next').disabled=index===24;};$('#window-next').onclick=()=>{index=Math.min(24,index+1);update();};$('#window-bubble').onclick=()=>update('气泡：窗口和计数保持。');$('#window-reset').onclick=()=>{index=0;update();};update();}
 }
 function renderUltra(id){
  const st=s.ultra[id]||={draft:'',answers:Array(7).fill(''),hints:0,checks:Array(4).fill(false),attempts:[]};const concept=id==='concept';
  const questions=['Gx（signed整数）','Gy（signed整数）','L1幅值','threshold=300时edge','容纳Gx/Gy需要的signed位数','容纳L1安全上界需要的unsigned位数','4级寄存器：E0采入，哪一边沿后级4可见？填E数字'];
  const expected=['80','240','320','255','11','11','E3'];
  const score=a=>a.reduce((n,x,i)=>n+(String(x).trim().toUpperCase()===expected[i]?1:0),0);
  const checks=concept?['我写出了方向和与差分过程','我说明了signed与unsigned范围','我画出了逐边沿valid与数据','我未用网页评分冒充工具仿真']:['独立规格、模块图、位宽和延迟表齐备','外部RTL仿真覆盖气泡、行帧边界与复位','Python/RTL数量、坐标和值逐一一致','综合/实现或未执行限制已明确记录'];
  host.innerHTML=header('ULTRA IMAGE · '+(concept?'概念检验':'独立工程'),concept?'给定窗口，推导算术与时序':'从接口规格独立设计流式 Sobel',concept?'窗口为[10,20,30;40,50,60;70,80,90]，阈值300，strict >，L1。流水线四级无阻塞，E0为输入被第一级采入的边沿。':'规格：RGB888、参数W/H≥3、同一时钟域、valid-only允许气泡、同步低有效复位、有效内部裁剪、L1、strict >。独立设计，不提供完整项目实现。')+(concept?panel('独立作答',questions.map((q,i)=>`<label class="fpga-field" for="ultra-answer-${i}">${e(q)}</label><input class="lab-input" id="ultra-answer-${i}" value="${e(st.answers[i]||'')}" maxlength="100">`).join('')):panel('交付与验收',list(['需求分析与输入输出协议，明确帧同步和跨域责任。','独立写RAM/窗口/算术模块及top，提交源码位置和关键设计理由。','固定向量与整图自检，给出第一处失败定位方法。','提交真实比较与工具证据；未执行的上板不声明成功。'])))+panel('推导 / 实现 / 证据',field('image-ultra-draft','独立工作记录（至少40字）',st.draft,12000)+`<div class="checklist">${checks.map((x,i)=>`<label><input type="checkbox" data-image-check="${i}" ${st.checks[i]?'checked':''}>${x}</label>`).join('')}</div>`)+panel('分级提示',`<p>先写40字分析才可展开；使用提示的本次记录标为辅助。</p>${['先独立分正负方向和，并把数据与标签画在同一时序表。','窗口中心比当前输入早一行一列；同步RAM响应必须和当前像素延迟对齐。','用常量、正负阶跃、气泡、两帧和复位逐层验证，最后再比较整图。'].slice(0,st.hints).map(x=>`<p>${e(x)}</p>`).join('')}<button id="image-hint" class="secondary-button" ${st.hints>=3?'disabled':''}>展开下一条提示 (${st.hints}/3)</button>`)+`<div class="button-row"><button id="image-submit" class="primary-button">提交本次检验</button><button id="image-new" class="secondary-button">保存草稿并开始新尝试</button></div><div id="image-feedback" class="feedback" role="status"></div>`+panel('最近五次记录',st.attempts.slice().reverse().map(a=>`<details class="answer-details"><summary>${e(new Date(a.at).toLocaleString())} · ${a.assisted?'辅助':'独立'} · ${concept?score(a.answers)+'/7 概念正确':'工程材料记录，待外部复核'}</summary><pre class="ultra-code">${e(a.draft)}</pre>${concept?`<p>你的答案：${e(a.answers.join(' / '))}</p>`:''}</details>`).join('')||'<p>尚无检验记录。</p>');
  if(concept)questions.forEach((_,i)=>$('#ultra-answer-'+i).oninput=ev=>{st.answers[i]=ev.target.value;persist();});$('#image-ultra-draft').oninput=ev=>{st.draft=ev.target.value;persist();};host.querySelectorAll('[data-image-check]').forEach(el=>el.onchange=()=>{st.checks[+el.dataset.imageCheck]=el.checked;persist();});$('#image-hint').onclick=()=>{if(st.draft.trim().length<40){toast('先写至少40字独立分析');return;}st.hints++;save();render(api,route);};
  const snapshot=()=>{st.attempts.push({at:new Date().toISOString(),draft:st.draft,answers:st.answers.slice(),assisted:st.hints>0,checks:st.checks.slice()});st.attempts=st.attempts.slice(-5);};$('#image-submit').onclick=()=>{if(st.draft.trim().length<40||!st.checks.every(Boolean)||concept&&st.answers.some(x=>!x.trim())){toast('先补齐答案、40字工作记录与四项自查');return;}snapshot();save();render(api,route);$('#image-feedback').textContent=concept?`本次 ${score(st.answers)}/7。核对：Gx=80，Gy=240，L1=320，edge=255；梯度signed11，幅值unsigned11；E0→级1，E1→级2，E2→级3，E3→级4。工程正确性仍需外部验证。`:'工程材料已保存；网页未编译RTL或执行Efinity，需按证据外部复核。';};$('#image-new').onclick=()=>{if(st.draft.trim())snapshot();s.ultra[id]={draft:'',answers:Array(7).fill(''),hints:0,checks:Array(4).fill(false),attempts:st.attempts};save();render(api,route);};
 }
}
root.IC_PROJECT={isRoute,nav,bridge,render};
})(window);
