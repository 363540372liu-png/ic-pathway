(function(root){
const D={};const add=(id,d)=>D[id]=d;
add('01',{
 goal:'能把一张图解释成像素、通道和坐标，并用邻域变化说明边缘。',
 sections:[['WHY · 边缘保留了什么','边缘通常出现在局部亮度变化明显的位置。轮廓有助于后续分析，但“亮”不等于“边缘”：一片均匀的白色区域内部没有梯度，而黑白交界处梯度很大。噪声也会产生快速变化，所以基线先验证确定的合成图，不以一张看起来漂亮的摄像头画面判断算法正确。'],['WHAT · 图像是一组有顺序的样本','分辨率 W×H 表示每行 W 个像素、共 H 行。采用零起点坐标 x=0..W-1、y=0..H-1。RGB888 给每个像素三个0..255通道，总共24位。灰度给每个像素一个强度值，通常本项目存成8位。读取图片库时必须确认返回RGB还是BGR，文件显示效果一致也不代表字节顺序一致。'],['HOW · 用小邻域估计变化','3×3核给中心周围九个位置各一个权重，逐点乘再求和。我们固定使用课程列出的Sobel方向，不翻转核，等价于常见图像工具里的相关计算约定。只说“卷积”而没有明确核方向会让Gx/Gy符号反转；幅值可能不变，逐级验证却会不一致。'],['硬件意义','软件可以任意索引图像矩阵，FPGA接口每拍只能接收到约定的一组样本。RGB三通道是24根逻辑数据线；图像坐标通常由有效像素计数得到。3×3邻域需要过去的行和列，不能只看当前一个像素。当前先手算，不急着写摄像头或HDMI。']],
 code:{name:'Python · 3×3 灰度样本',text:'image = [[10,20,30], [40,50,60], [70,80,90]]\ncenter = image[1][1]   # 50\n# 坐标使用 image[y][x]，不要混淆行与列'},
 example:'3×3样本的中心是50；横向每步增加10，纵向每步增加30。方向变化强弱不同，后续Sobel会得到Gx=80、Gy=240。',diagram:['RGB像素 / 坐标','灰度强度','邻域变化','梯度 / 边缘'],
 errors:['把一个像素当作一整行。','把均匀白色区当成强边缘。'],debug:['先显示像素数值和坐标。','用常量、横向阶跃、纵向阶跃三种输入隔离问题。'],exercise:'画出一张4×3灰度图，并标出(2,1)的邻域；说明边界像素为什么缺少完整3×3窗口。',checkpoint:['分清x/y与行列','说明RGB888的位数','解释常量图内部梯度为0']});
add('02',{
 goal:'定义灰度浮点参考与整数实现，测量近似误差，避免把它们当作同一个算法。',
 sections:[['WHY · 为什么绿色权重最大','常见关系Gray≈0.299R+0.587G+0.114B按不同通道加权，它是常用亮度近似，不是对所有色彩空间通用的物理亮度定义。本项目先固定8位RGB编码和这组系数，不混入gamma或摄像头色彩校正。合同越明确，软件与RTL越容易一致。'],['WHAT · 系数乘256后成为整数','77、150、29的和恰好256。先计算整数和再右移8，得到向下取整的8位结果。77/256并不等于0.299，因此即使不截断也存在系数近似误差；右移又引入小于1灰度级的向下取整误差。不要先把每个乘积右移再相加，否则会累加多次截断。'],['HOW · 两层比较','第一层用浮点公式评估整数灰度的最大绝对误差与平均绝对误差，接受容限需要写进规格。第二层RTL必须与已经冻结的整数公式逐像素完全相等。浮点与整数不完全相等不必然是RTL错误；两个不同舍入规则的整数实现也不能混用。'],['硬件意义与位宽','通道最大255。77R≤19635，150G≤38250，29B≤7395，总和≤65280，小于65536，因此无符号16位和足够。输出取sum[15:8]。移位实现常系数时要先把8位输入扩成16位，再移位；先在8位上左移会丢掉高位。']],
 code:{name:'Python · 浮点与硬件整数版本',text:'r,g,b = 255,0,0\nreference = .299*r + .587*g + .114*b\ninteger = (77*r + 150*g + 29*b) >> 8\nprint(reference, integer, abs(reference-integer))\n# 76.245, 76, 0.245（浮点打印有舍入误差）'},
 example:'纯红→76，纯绿→149，纯蓝→28，纯白→255；这些是本项目向下取整规则的结果。不要套用图像库的默认舍入后要求数值完全一样。',diagram:['RGB888','16位常系数乘加','统一右移8','8位灰度 + valid'],errors:['RGB/BGR通道颠倒。','乘积提前截断。','擅自加128改变舍入规格。'],debug:['先测黑白和三个纯色。','打印三个乘积与总和。','分别报告近似误差与RTL精确比较。'],exercise:'手算R=10、G=20、B=30的两种灰度；再写Python遍历一组颜色并报告最大误差。',checkpoint:['写清floor规则','推导65280上界','区分近似误差和实现错误']});
add('03',{
 goal:'从九个像素和两个核推导有符号梯度，不靠记忆代码。',
 sections:[['WHY · 看方向而不是只看亮度','Gx比较右侧与左侧，Gy比较下侧与上侧。中间行/列权重2加强邻近信息。我们定义x向右、y向下，按所示核直接对窗口求加权和。相反方向的边缘会给出负梯度，所以绝不能把梯度定义为8位无符号像素。'],['WHAT · 固定本项目核约定','Gx的三行为[-1,0,+1]、[-2,0,+2]、[-1,0,+1]；Gy的三行为[-1,-2,-1]、[0,0,0]、[+1,+2,+1]。P11在左上，P33在右下。Gx=-P11+P13-2P21+2P23-P31+P33；Gy=-P11-2P12-P13+P31+2P32+P33。中心P22不参与这两个差分和。'],['HOW · 先正负分组','Gx=(P13+2P23+P33)-(P11+2P21+P31)。先分别求左右两组非负和，再在足够宽的signed信号上相减。Gy同理求下减上。乘2可以移位，但移位前必须扩位。对小窗口先手算，再用程序验证，最后才写RTL；这样错误能定位到公式而不是整个系统。'],['硬件意义','常数乘2可表示为接线移位，剩余主要是加法器、减法器和寄存器。组合逻辑可以同时计算Gx/Gy；它们不是CPU依次执行的两段循环。是否需要多级寄存器取决于目标频率和实现后时序，不能仅凭表达式短就宣布一拍满足。']],
 code:{name:'Python · 有符号整数梯度',text:'p11,p12,p13,p21,p22,p23,p31,p32,p33 = range(10,100,10)\ngx = -p11+p13-2*p21+2*p23-p31+p33\ngy = -p11-2*p12-p13+p31+2*p32+p33\nprint(gx, gy)  # 80, 240'},example:'三行均为[0,0,255]时Gx=1020、Gy=0；左右反过来Gx=-1020。常量窗口的两个梯度都为0。',demo:'sobel',diagram:['九像素窗口','右减左 → Gx','下减上 → Gy','保留符号'],errors:['把行列或核方向翻转。','负结果存unsigned。'],debug:['打印九点而不是只打印中心。','分别对比左右和、上下和。','用正负阶跃检查符号。'],exercise:'独立计算[10,20,30;40,50,60;70,80,90]的Gx/Gy，再改变P22，预测是否变化。',checkpoint:['写出两个核','解释中心权重0','能用正负阶跃验方向']});
add('04',{
 goal:'理解L2幅值、L1近似和严格阈值的区别，冻结比赛基线。',
 sections:[['WHY · 避免一开始引入平方根硬件','数学幅值sqrt(Gx²+Gy²)考虑两个方向的合成长度。实现它可能引入较宽平方、求和和平方根模块。基线使用|Gx|+|Gy|，只需求绝对值和加法，便于初学者验证。它改变了算法的数值尺度，不是数学上完全等价的变形。'],['WHAT · 近似的偏差有方向性','同一Gx/Gy下L2≤L1≤sqrt(2)×L2。一个方向为0时两者相同，两方向接近时L1相对更大。因此用同一个阈值可能产生不同的二值边缘；应比较边缘密度、噪声与目标轮廓，再选择阈值，不把差异一律当bug。'],['HOW · 明确边界条件','本项目采用magnitude>threshold输出255，否则0，等于阈值时输出0。阈值是原始未归一化L1的11位参数，范围0..2047。若先把幅值显示缩放到8位再判阈值，就变成另一个模型。阈值变化也应定义在哪个帧边界生效，避免一帧上下部分使用不同设置。'],['硬件意义','符号位控制求绝对值，两个无符号幅值相加，再由比较器控制0/255多路选择器。对11位signed一般数值-1024要谨慎处理绝对值，但Sobel合法输入范围仅到-1020。中间值保持足够位宽，阈值之后才变成8位二值图像。']],code:{name:'Python · 两种幅值不是同一规格',text:'import math\ngx,gy,threshold = 80,240,300\nl2 = math.hypot(gx,gy)\nl1 = abs(gx)+abs(gy)\nedge = 255 if l1 > threshold else 0\n# L2≈252.98, L1=320, 本项目edge=255'},example:'L1=300、threshold=300输出0；L1=301输出255。降低阈值通常保留更多细节，也容易把噪声当边缘。',diagram:['signed Gx / Gy','绝对值','11位 L1','> threshold → 0 / 255'],errors:['把>写成>=。','在幅值饱和之后比较阈值。'],debug:['专测阈值前后与恰好相等。','保存未缩放幅值。'],exercise:'选择三个阈值，对同一图计算边缘像素数量，说明哪一个更适合目标而不是只说好看。',checkpoint:['区分L1与L2','说明相等时输出','记录阈值单位与更新时机']});
add('05',{
 goal:'交付可重跑的软件Golden、数值规格和expected像素文件。',
 sections:[['WHY · Golden是合同而不是漂亮图片','Golden Reference用于定义每个输入应该产生什么输出。必须固定通道顺序、位宽、系数、舍入、核方向、幅值、阈值和边界规则。若Python随意升级算法而RTL仍按旧规则运行，比较失败并不能说明谁错；记录源码版本和参数是验证的一部分。'],['WHAT · 两条软件支路','浮点支路使用浮点灰度与L2幅值，帮助评估近似效果；整数支路使用floor灰度与L1，作为RTL逐像素匹配的唯一基准。先报告灰度最大/平均误差与二值差异数量，再写明为什么接受这种近似。不能要求不同数学算法的输出完全一样。'],['HOW · 可下载工作流','下方工具包自带确定性的8×6 RGB图，无需第三方库即可运行。它输出原图PPM、灰度PGM、原始L1幅值PGM、二值PGM、浮点参考边缘、metrics.json、input.hex、expected.txt和stages.csv。读取PNG/JPEG时才需本地安装Pillow；网页没有运行Python或读取你的本地图像。'],['边界与文件格式','基线采用valid crop：只计算有完整邻域的内部中心(x=1..W-2,y=1..H-2)，输出数量(W-2)(H-2)。expected.txt每行是原图中心坐标x y edge，按行优先输出。不要把输出裁剪后的坐标从0重新编号。显示端若需要原分辨率，补边由单独适配层完成并明确协议。']],code:{name:'本地终端 · 真实Python执行',text:'python golden.py --out results --threshold 300\n# 可选：安装Pillow后读取自己的图\npython golden.py --image input.png --out results\n# RTL仿真生成rtl.txt后再比较，不能复制expected冒充RTL\npython compare.py results/expected.txt rtl.txt'},example:'自带8×6图应输出24个有效中心像素。输出文件为空或只有23行，即使画面看起来正常也不能通过精确比较。',downloads:true,diagram:['同一RGB输入','浮点参考 / 整数Golden','数值差异与规格','expected.txt / stages.csv'],errors:['两条支路分别使用不同边界规则。','把可视化归一化后的图当expected。'],debug:['从3×3与5×5小图开始。','检查行数、坐标顺序、阈值和版本。'],exercise:'运行自带向量，保存metrics和24行expected；再用常量图、正负阶跃和固定随机种子图做回归。',checkpoint:['能从空目录复现','有参数与版本记录','不把浮点近似差异当RTL不一致']});
add('06',{
 goal:'把二维数组计算转换为每拍一个被接受的像素事件。',
 sections:[['WHY · 摄像头不会一次交出整幅矩阵','软件拥有完整图后可以反复image[y][x]随机访问；流式硬件接收p0、p1、p2…，过去的数据若没有保存就不存在。要在当前像素到来时计算一个3×3中心，必须知道历史两行与前两列。行缓存是时间换空间的安排，不是另一个摄像头协议。'],['WHAT · 基线接口','算法核只约定clk、低有效同步rst_n、RGB888 pixel_in、pixel_in_valid及相同域的帧/行标志。无背压基线下每个valid高的边沿接受一个像素；valid低时输入数据忽略。SOF/EOL是否存在、在哪个像素对齐由团队接口表决定，不绑定某种摄像头总线。'],['HOW · 计数按接受事件更新','x在每个被接受像素后加1，到W-1回0并使y加1；消隐拍不推进。复位或受约定的帧开始事件重置坐标和填充状态。算术流水线的valid却每个时钟都前进，包括气泡，所以坐标计数与流水线移位不能共享一个粗糙的总使能。'],['硬件意义','行存储和窗口以“像素接受”为节奏，算术级以系统时钟为节奏。无背压意味着团队必须保证下游带宽；后来加入ready时，要定义全链路停顿、缓存容量和标签保持，不能只给接口加一根线。']],code:{name:'可综合片段 · 仅坐标计数，不是完整顶层',text:'// W已定义且x足够宽；帧高与SOF策略另行定义\nalways @(posedge clk) begin\n  if (!rst_n) begin x<=0; y<=0; end\n  else if (pixel_in_valid) begin\n    if (x==W-1) begin x<=0; y<=y+1\'b1; end\n    else x<=x+1\'b1;\n  end\nend'},example:'W=4，输入valid为1,1,0,1,1，对应有效像素x为0,1,2,3；第三个时钟是气泡，不属于任何新列。',diagram:['团队 Camera Interface','pixel + valid + 标签','我的单时钟算法流水线','processed_pixel + valid'],errors:['每个时钟都增加列号。','把valid用作时钟。'],debug:['用像素序号替代真实照片。','同时打印接受序号、x/y和时钟编号。'],exercise:'画W=4且包含两次消隐的两行输入时序，标出每个被接受像素的坐标。',checkpoint:['区分时钟拍与像素序号','定义valid-only假设','给团队列出SOF/EOL问题']});
add('07',{
 goal:'逐步推导灰度、梯度、绝对值和幅值的安全位宽。',
 sections:[['第一步 · 从非负和开始','灰度通道0..255；整数加权和最大(77+150+29)×255=65280，用16位。Sobel右侧和P13+2P23+P33最大4×255=1020，左侧和同样最大1020，用10位无符号可容纳。现在先写上下界，暂时不看最终代码。'],['第二步 · 减法引入符号','Gx右减左、Gy下减上范围均为-1020..1020。signed10位只能表示-512..511，不够；signed11位表示-1024..1023，够用。把非负和转成signed之前需在最高位补0，否则值大于511的10位数会被解释成负数。'],['第三步 · 幅值与保守边界','|Gx|和|Gy|各最大1020，独立上界相加为2040，因此11位unsigned（0..2047）安全。两者共用同一个窗口，不能同时达到各自最大值；精确可达的L1上界为1530。理由是|Gx|+|Gy|等于四种±Gx±Gy的最大值，合成核正权重之和均为6，6×255=1530。保守2040用于选位宽没有错，但不能声称它是可达最大样本。'],['第四步 · 扩位要发生在运算前','8位P左移1如果先在8位中计算，最高位已经丢掉。先补零为11位signed再移位，后面的加减都维持同一signed宽度。阈值前不要截断到8位；截断、舍入、饱和分别是不同的硬件和算法规格。']],code:{name:'可综合片段 · 已声明p11/p21/p31与11位signed临时线',text:'wire signed [10:0] a = $signed({3\'b000,p11});\nwire signed [10:0] b = $signed({3\'b000,p21});\nwire signed [10:0] c = $signed({3\'b000,p31});\nwire signed [10:0] left_sum = a + (b <<< 1) + c;\n// right_sum同样构造；gx=right_sum-left_sum，保持11位signed'},example:'P=255时扩位再移位得到510。若误用8位截断，510变254，会把梯度与阈值判断同时破坏。',diagram:['8位灰度','10位非负方向和','11位signed Gx/Gy','11位unsigned L1'],errors:['把2040当作真实可达的L1峰值。','拼接后忘记signed转换。'],debug:['列每个节点的类型、范围、舍入规则。','用极端0/255窗口检查溢出。'],exercise:'推导各级位宽并构造一个L1=1530的窗口。再写一个会暴露8位左移错误的输入。',checkpoint:['signed范围正确','说明2040保守界与1530紧界','扩位先于算术']});
add('08',{
 goal:'为图像算术逐级安排寄存器，并解释填窗延迟与每拍吞吐。',
 sections:[['WHY · 一长串加减比较不一定满足时钟','灰度乘加、Sobel方向和、绝对值与阈值可以拆成若干寄存器级。每级逻辑更短，但数据要经历更多采样边沿。我们先定义时序合同，再按报告调整分级；不能在“实时”旁写字面上的零延迟。实时通常指持续处理速度与系统需求相符。'],['WHAT · 两类等待相加','第一类是行缓存填充：零起点输入(row=2,col=2)到来时才第一次拥有完整3×3邻域；无气泡时这是第2W+3个接受像素。第二类是同步RAM与算术寄存器延迟。以四个算术寄存器级为例，输入在E0被第一级采入，第四级在E3后可见；若把输入边沿之前称为周期0，也常称四级延迟。必须用具体边沿表消除术语差异。'],['HOW · valid、坐标与参数一起走','灰度级处理输入有效位；窗口级在有效区域才产生window_valid；之后Gx/Gy、mag、edge各级传播有效位与中心坐标。valid低时也传递气泡。阈值若动态变化，选择帧边界锁存，或和像素标签同时流水，避免比较时用了另一时刻的阈值。'],['吞吐条件','无背压且各级每拍可接收时，内部有效区域可每拍输出一个像素；每行前两列及首两行仍无有效窗口，帧的平均输出数量是(W-2)(H-2)。吞吐、频率、分辨率、帧率和消隐一起决定是否能接入团队视频流。']],code:{name:'可综合片段 · valid延迟，无背压',text:'reg [3:0] v;\nalways @(posedge clk) begin\n  if (!rst_n) v <= 4\'b0000;\n  else v <= {v[2:0], pixel_in_valid};\nend\n// 数据、中心坐标必须经过对应四级；不能只延迟v。'},example:'接受A、B、气泡、C后，四级输出仍是A、B、气泡、C的顺序；气泡不应变成重复B。',diagram:['Gray / 寄存器','行缓存与Window / 标签','Gx/Gy → abs+sum','threshold → output valid'],errors:['只数算术级，遗漏RAM。','把零等待误叫实时。'],debug:['每级临时带样本编号。','对比每个边沿前后的数据。'],exercise:'画一个灰度级、同步RAM级、梯度级、幅值级和阈值级的具体边沿表，标出气泡与首个中心坐标。',checkpoint:['分清首窗等待与流水延迟','能解释每拍吞吐条件','标签对齐']});
add('09',{
 goal:'用历史两行和当前行解释Sobel所需存储，而不是背一个FIFO模板。',
 sections:[['WHY · 当前像素只占九分之一','收到(row,col)时，窗口右下角是当前像素；右上角来自(row-2,col)，右中角来自(row-1,col)。每行还要保留同一行前两个列值。两行缓存提供三行同列的一列数据，三个短移位链再把这列扩展成3×3窗口。'],['WHAT · 缓存保存的时间跨度','宽W的灰度图，每一行需要W×8位，历史两行至少2W×8位。640宽时10240位，不包含窗口寄存器和控制。缓存延迟按被接受像素数计算；接口消隐拍不应该写入假像素，否则“上一行”就混入无效数据。'],['HOW · 先画三行同列','用W=5并让pixel=y×5+x，接收(2,2)即像素12时，窗口应为[0,1,2;5,6,7;10,11,12]，中心原图坐标是(1,1)。这张图足够暴露多数一行或一列偏移，远比直接观察相机照片容易。同步RAM的额外一拍仍需显式对齐，不包含在理想像素编号关系里。'],['硬件意义','行缓存通常映射到片上RAM，窗口只需九个较小寄存器。可以采用两级延迟行、轮换行存储等架构，但每一种都必须定义读写端口和冲突行为。当前学习先选一种可验证的同步读方案，不引入DDR帧缓存。']],code:{name:'Python · 理想窗口索引模型（不是RTL RAM时序）',text:'W=5; row,col=2,2\nwindow=[[ (row-2+dy)*W + col-2+dx\n          for dx in range(3)] for dy in range(3)]\n# [[0,1,2],[5,6,7],[10,11,12]]'},example:'第一次完整窗口需要至少接收2W+3个有效像素；W=5时是第13个。空白拍拉长时钟等待，但不改变有效像素数量。',demo:'window',diagram:['前两行 RAM → 上行同列','前一行 RAM → 中行同列','当前像素 → 下行同列','三路短移位 → 3×3'],errors:['只存一行。','把消隐也写进缓存。'],debug:['使用递增编号图。','先查三行同列，再查九点窗口。'],exercise:'为W=6计算两行存储位数和首窗所需有效像素数，画出接收(2,2)时九点编号。',checkpoint:['区分行存储与窗口移位','说明中心坐标偏移','首窗计数正确']});
add('10',{
 goal:'把同步RAM的读响应、当前像素与坐标对齐，定义可实现的行缓存接口。',
 sections:[['WHY · 软件下标掩盖了读延迟','在Python写prev[x]就拿到结果，但同步BRAM先采地址，之后输出寄存值。下游在同一个边沿读取输出会得到上一次地址的数据。必须把当前像素、x/y、valid一起延迟到RAM读结果对应的时刻，再组成一列；否则每行都可能斜一像素。'],['WHAT · 两级行延迟的合同','先定义line_buffer输入gray/valid/x/y，输出top、middle、bottom及column_valid和延迟标签。它只负责提供同列的三行像素，不负责完成3×3横向窗口。两行历史数据未填满时column_valid必须为0，即使RAM恰好输出某个已知数也不能认为它属于当前帧。'],['HOW · 冲突模式不是猜的','一种行延迟实现会在同一地址读旧值并写新值，这要求清楚的read-first行为。RTL仿真非阻塞赋值看起来读旧值，不自动保证厂商存储原语的所有冲突模式相同。选择目标器件推荐的推断模板或明确双端口/轮换存储安排，综合后核对资源和读写行为；不满足时先修实现而不是篡改Golden。'],['硬件意义','容量决定存储块数量，端口数量和读延迟决定数据调度。复位时清控制计数与valid，利用前两行重新填充屏蔽旧内容；整块RAM逐项同步清零可能阻止存储推断。帧宽改变必须重新核对地址宽度和缓存配置。']],code:{name:'可综合片段 · 同步读写语义，需声明mem/addr',text:'always @(posedge clk) begin\n  if (accept) begin\n    old_pixel <= mem[addr];\n    mem[addr] <= new_pixel;\n  end\nend\n// RTL为旧值语义；真实BRAM冲突模式必须核验。\n// old_pixel对应的new_pixel和addr需要延迟匹配。'},example:'连续请求列0、1、2时，下游不能把读取列0的响应标签写成列1。用“地址=数据”一眼能看出偏移。',diagram:['地址请求 / 当前像素','同步RAM读寄存器','延迟当前像素 / x/y','同列三行响应'],errors:['假设BRAM组合读。','大数组复位循环。'],debug:['单独测RAM读写模式。','打印请求地址与响应标签。','看综合是否真的使用BRAM。'],exercise:'选择一种行缓存存储方案，画出读写端口、请求和响应两拍表，并列出必须由Efinity报告核实的事项。',checkpoint:['接口有column_valid','当前像素与RAM响应对齐','冲突模式有证据']});
add('11',{
 goal:'逐个接受像素构造窗口，严格处理行边界、初始填充和有效区域。',
 sections:[['WHY · 行缓存只给一列','上、中、下三行同列数据到来后，每行各自左移：旧中列成为左列，旧右列成为中列，新值进入右列。三行同时更新后才代表同一时刻的邻域。不能把三个always的执行顺序当作逐行软件过程，非阻塞赋值使用边沿前的旧值。'],['WHAT · 窗口的中心不是当前输入','当前接受的输入在窗口右下角(row,col)，中心是(row-1,col-1)。零起点row≥2且col≥2才有完整窗口；第一、二行和每行第一、二列都不能输出有效结果。基线采用裁剪而不是补零，输出数量与Golden严格一致。'],['HOW · 行边界要清楚','每行col=0可清横向历史，或保留旧值但在前两列屏蔽valid，确保到col=2时旧行数据已经完全被新行覆盖。无论哪种，不能在换行后立即宣称有效。所有判断必须使用与RAM读响应同拍的延迟行列号，而不是未经延迟的输入计数。'],['逐拍理解','接收W=5的像素12时得到第一个窗口；接收13时每行向左移一格得到[1,2,3;6,7,8;11,12,13]。到下一行的像素15，窗口无效，直到17才有新一行的第一个有效窗口。出现气泡时存储与窗口不前移，但下游算术valid应继续传递0。']],code:{name:'可综合片段 · 一行的横向窗口移位',text:'if (column_valid) begin\n  p11 <= p12;\n  p12 <= p13;\n  p13 <= top_pixel;\n  // 中、下行同理，各用对应同列像素。\nend\n// window_valid需要对齐更新后的九点与延迟行列。'},example:'W=5、H=5应有9个有效窗口。首个中心(1,1)，末个中心(3,3)，没有(0,0)或(4,4)输出。',demo:'window',diagram:['新三行同列数据','每行左移一格','九点 / 中心坐标','valid crop 条件'],errors:['跨行串窗。','把更新前九点配给更新后valid。'],debug:['逐窗比较九个编号。','检查每行前两列。','加入不规则valid气泡再比。'],exercise:'手写W=5整帧有效中心坐标序列，独立画出输入12、13、15、17四次接受事件后的窗口。',checkpoint:['正确屏蔽初始行列','中心坐标正确','气泡与换行不串数据']});
root.IC_PROJECT_CONTENT=D;
})(window);
