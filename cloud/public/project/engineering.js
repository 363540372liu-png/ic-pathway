(function(root){
const D=root.IC_PROJECT_CONTENT;
function rtl(id,name,goal,sections,code,steps,example,errors,debug,exercise,checkpoint,diagram){D[id]={goal,sections,code:{name:'可综合逻辑片段 · '+name+'（非完整模块，先独立补接口）',text:code},steps,example,errors,debug,exercise,checkpoint,diagram};}
rtl('12','rgb2gray','把整数灰度公式实现成带valid的单级或分级RTL，并与Python严格比较。',[
 ['接口与硬件预测','输入clk、同步低有效rst_n、24位pixel_in和pixel_in_valid，输出8位pixel_out及pixel_out_valid。先写清通道位段，例如[23:16]为R。预测至少有乘加逻辑、灰度寄存器与有效寄存器；不要因为公式只有一行就忽略寄存器边界和运算位宽。'],
 ['逐段搭建','先单独形成三个16位乘积与16位总和，再按冻结规则取[15:8]。如果为满足频率拆成乘积级与求和级，valid也需要两级。代码片段故意只给算术，接口、复位、寄存器和延迟声明由你完成，避免把整套工程复制后却说不清第一拍。'],
 ['波形与独立验证','输入纯红、绿、蓝、白并插入气泡。输出应按接口延迟给出76、149、28、255，气泡不产生额外valid。用本地Python生成向量，通过RTL测试台采样边沿后比较；保存草稿不等于编译或仿真成功。']],
 'wire [15:0] r16={8\'d0,pixel_in[23:16]};\nwire [15:0] g16={8\'d0,pixel_in[15:8]};\nwire [15:0] b16={8\'d0,pixel_in[7:0]};\nwire [15:0] sum = r16*16\'d77 + g16*16\'d150 + b16*16\'d29;\n// 在你定义的有效边沿把sum[15:8]注册到pixel_out。',
 ['Step 1 · 写接口、通道顺序、复位和一拍延迟约定。','Step 2 · 画乘加与寄存器，预测纯色结果。','Step 3 · 补上算术、输出寄存器和valid逻辑。','Step 4 · 在外部仿真器运行纯色与随机向量。','Step 5 · 波形同时看输入颜色、sum、输出与valid。','Step 6 · 独立实现带气泡的自检测试台，不复制Golden作为RTL结果。','Step 7 · 记录位宽、延迟、复位、随机测试与失败注入证据。'],
 '连续输入红/气泡/白，输出valid模式应为1/0/1的相应延迟版本；无效拍输出数据允许保持。',['输出用了错误字节顺序。','测试台在NBA更新前采样。'],['先比较sum。','再查取位。','最后查valid延迟。'],'独立提交rgb2gray与测试台，描述你为什么选择一拍或多拍，以及所有中间位宽。',['纯色与最大值通过','气泡与复位通过','整数Golden精确一致'],['RGB888','16位乘加','寄存灰度 / valid']);
rtl('13','line_buffer','独立搭建同列三行接口，先证明存储行为再接Sobel。',[
 ['接口与责任','输入8位gray、accept、当前行列/帧开始信息，输出同列top/middle/bottom、column_valid和配套坐标。模块内部可以有多拍延迟，但必须写在接口表中。不能把缓存内部状态机、摄像头行同步和Sobel算术全部混在同一个巨大always里。'],
 ['从小容量开始','先用W=5、H=5，存储数据就是0..24递增序号。分别验证第一行写入、第二行读出与第三行两行历史可用。若采用同地址读写旧值，检查仿真语义及厂商BRAM映射；如果采用轮换行RAM，则明确哪一块读、哪一块写及行结束交换时机。'],
 ['波形解释与独立实现','波形画addr_request、ram_response、current_delayed、column_valid和x/y。三行值需要指向同一列，不是“看起来每个都在变化”就对。复位不强制清空整块存储，只把历史行有效状态清零，重新收足两行后再输出有效列。']],
 '// 行缓存接口骨架：请独立选择读写架构\n// input clk,rst_n,gray_valid; input [7:0] gray;\n// output [7:0] top,middle,bottom;\n// output column_valid; output delayed_x,delayed_y;\n// 不在本片段给出完整实现；先交时序表。',
 ['Step 1 · 定义列响应延迟和帧重启行为。','Step 2 · 画RAM端口和两行存储容量。','Step 3 · 先写单行延迟，再增加第二行。','Step 4 · 外部仿真用递增序号和气泡。','Step 5 · 核对每列top/middle/bottom和延迟坐标。','Step 6 · 独立扩大W并覆盖新帧/复位中断。','Step 7 · 综合确认BRAM映射，保存冲突模式依据。'],
 '收到输入(row=2,col=2)的列响应应为top=2,middle=7,bottom=12；这是列，不是完整窗口。',['同步RAM响应错配当前像素。','复位后输出旧帧缓存。'],['单测一行。','只比较列响应，不先接梯度。'],'写出一种可实现架构和最小测试台，提交至少两种帧宽、气泡与复位覆盖。',['三行同列一致','初始历史屏蔽','资源推断已记录'],['gray / accept','行延迟1','行延迟2','同列响应 + 标签']);
rtl('14','window_3x3','在正确列响应之上搭九点窗口，输出有效中心坐标。',[
 ['接口与硬件预测','输入来自line_buffer的三行同列响应及其valid/x/y，输出九个灰度点、window_valid与center_x/center_y。九点可拆端口或打包总线，但团队必须冻结打包次序。预测三条长度3的横向移位链，不能再凭当前输入计数给它贴标签。'],
 ['先正确再省寄存器','在每个有效列响应到来时，同步更新三行移位链。window_valid来自延迟的row≥2、col≥2，同时必须与更新后的窗口在后级采样时一致。使用输出寄存器时，考虑消费者在同边沿只能看到旧窗口；valid应以相同寄存器边界传递。'],
 ['独立验证窗口而非图片','测试台转储每次window_valid的九个像素和中心坐标，让Python逐窗核对。先使用5×5编号图，再加入随机气泡、每行边界、两帧背靠背和中途复位。窗口错误应在进入Sobel前解决，避免梯度放大后更难定位。']],
 '// 每个有效同列响应，三行同时移位\nif (column_valid) begin\n  p11<=p12; p12<=p13; p13<=top;\n  p21<=p22; p22<=p23; p23<=middle;\n  p31<=p32; p32<=p33; p33<=bottom;\nend\n// window_valid与center标签需要由你按时序表注册。',
 ['Step 1 · 写九点打包顺序与坐标定义。','Step 2 · 预测输入12和13对应的两个窗口。','Step 3 · 补齐移位、有效区域、标签与复位。','Step 4 · 用小图在外部仿真器跑逐窗比对。','Step 5 · 核对换行后两列无效及第3列恢复。','Step 6 · 独立覆盖气泡和帧切换。','Step 7 · 总窗口数必须为(W-2)(H-2)。'],
 '5×5输入产生9个完整窗口；第一个中心(1,1)，不是输入像素的(2,2)。',['窗口位段顺序反了。','中心标签偏1。'],['对九点逐项检查。','先找第一个错窗坐标。'],'独立提交window_3x3和窗口转储文件，并解释每行col=0时如何避免旧行污染。',['九点顺序正确','有效中心数量正确','行界/气泡/复位覆盖'],['同列三行 / valid','九寄存器移位','区域条件','窗口 + center + valid']);
rtl('15','sobel_core','在固定九点输入上证明signed梯度与L1，不依赖摄像头或行缓存。',[
 ['接口与硬件预测','输入九个8位无符号灰度点与window_valid，输出11位signed gx/gy、11位unsigned magnitude及valid。可以分为梯度与绝对值求和两级；明确每级延迟。先用固定窗口单测，完全可以在line_buffer完成之前推进这项工作。'],
 ['逐段搭建算术','先把像素扩为11位signed，再构造左右/上下和与相减。绝对值的输出是非负量；在相加前显式转为足够宽的unsigned。不要把11位signed正数容纳上界1023的类型继续用于最高1530的L1结果。'],
 ['波形解释','固定[10,20,30;40,50,60;70,80,90]，梯度级应出现80和240，后面的幅值级出现320。若同一个always里mag<=abs(gx)+abs(gy)，它计算的是旧gx/gy，这可能是有意的第二级，而不是语言错误；valid和标签必须对应。']],
 '// gx、gy为已注册的11位signed梯度\nwire [10:0] ax = gx[10] ? $unsigned(-gx) : $unsigned(gx);\nwire [10:0] ay = gy[10] ? $unsigned(-gy) : $unsigned(gy);\nwire [10:0] l1 = ax + ay;\n// 仅对本项目合法Sobel范围使用；mag寄存器与valid另行完成。',
 ['Step 1 · 冻结九点顺序、signed类型和输出延迟。','Step 2 · 手算常量、正负阶跃与斜边窗口。','Step 3 · 分别写梯度与幅值片段。','Step 4 · 外部仿真先跑固定窗口，再跑随机窗口。','Step 5 · 每级同时查看gx/gy/mag与valid。','Step 6 · 独立生成测试向量，至少注入一次错误验证测试台。','Step 7 · 比较每个中间值，不仅最终黑白像素。'],
 '合法极端窗口可达L1=1530；11位unsigned能容纳，11位signed则不能。',['mag误声明signed。','测试只有最终阈值，掩盖梯度小偏差。'],['检查方向和符号。','用极端窗口查宽度。'],'独立实现两级sobel_core和逐中间节点自检，解释为何不需要乘法器实现乘2。',['Gx/Gy符号正确','L1位宽正确','阶段valid匹配'],['九点','signed Gx/Gy 寄存','abs / unsigned sum','magnitude / valid']);
rtl('16','threshold','用明确的比较规则把幅值转换成二值边缘，并处理阈值更新时机。',[
 ['接口与硬件预测','输入11位unsigned magnitude、有效位和11位threshold；输出8位edge与有效位。比较器判断严格大于，再选择255或0。即使这一级简单，也需要和数据一起传播中心坐标，避免显示位置偏移。'],
 ['动态参数也需要协议','比赛基线可以先使用固定参数，后续阈值可调作为扩展。若运行中改变threshold，要约定每帧开始锁存还是每个像素携带快照；未经约定的即时更新可能让同一帧上下半部亮度不一致。先让固定阈值通过全部验证，再加控制接口。'],
 ['波形与测试','依次输入299、300、301且threshold=300，应得到0、0、255；中间插入无效拍不能重复输出。比较结果的延迟与valid延迟相同。不要先将magnitude饱和成8位，因为300以上的信息是这项比较必需的。']],
 'always @(posedge clk) begin\n  if (!rst_n) begin pixel_out<=8\'d0; edge_valid<=1\'b0; end\n  else begin\n    edge_valid<=magnitude_valid;\n    if (magnitude_valid) pixel_out <= magnitude>threshold ? 8\'d255 : 8\'d0;\n  end\nend',
 ['Step 1 · 写出输入输出类型与阈值单位。','Step 2 · 画比较器和结果寄存器。','Step 3 · 补齐代码与标签。','Step 4 · 外部仿真覆盖T-1/T/T+1和0/1530。','Step 5 · 观察数据与valid相同延迟。','Step 6 · 独立定义复位与阈值更新协议。','Step 7 · 保存比较边界用例结果。'],
 '阈值1530时，合法Sobel窗口不会得到边缘255，因为使用严格大于。',['>=与Golden不一致。','幅值提前饱和。'],['先固定threshold。','检查magnitude真实数值而不是灰度显示图。'],'独立写阈值模块并解释等于阈值时的结果，准备后续帧边界更新方案但暂不实现。',['边界比较正确','参数位宽明确','valid和标签保持'],['11位幅值','严格 > 比较','0 / 255 寄存','edge valid']);
rtl('17','edge_pipeline_top','把已单测的模块串成可复用算法核，先通过整图仿真再进入工具和板卡。',[
 ['接口与系统边界','顶层只消费统一的像素流，输出二值边缘与中心坐标/有效位；摄像头采集与显示时序属于团队依赖。你的接口必须声明RGB顺序、宽高、同一时钟域、帧重启、无背压条件和裁剪格式。不要把MIPI/HDMI协议塞进算法核去掩盖接口问题。'],
 ['按证据集成','先把rgb2gray与line_buffer连接，核对灰度与列；再接window核对九点；然后Sobel、幅值和threshold。每接一级都跑同一确定性输入并保留日志。若最终画面错，从第一处数值不一致回查，而不是同时改多个模块。'],
 ['延迟表与复位清空','给每段写输入采样边沿、输出可见边沿、valid与标签延迟；行缓存另记首窗填充条件。复位时清流水有效位与窗口填充状态，不能让旧帧残留输出。帧间无消隐也必须按约定重新计数并屏蔽首两行，后级可能还在排出上一帧尾部，需要标签或明确冲刷流水规则。']],
 '// 顶层连接计划，不是可直接编译的完整答案\n// RGB stream -> rgb2gray -> line_buffer -> window_3x3\n// -> sobel_core -> threshold -> edge stream\n// 每条连接列出：data宽度 / valid / x,y / latency\n// 你的任务：独立写端口、实例、连线与复位策略。',
 ['Step 1 · 冻结算法接口合同。','Step 2 · 画模块图与逐级延迟表。','Step 3 · 独立写顶层实例连线。','Step 4 · 外部仿真逐级检查，再跑整图。','Step 5 · 从SOF到首窗、尾窗与最后输出看完整波形。','Step 6 · 独立测试不同宽高、气泡、两帧和中断复位。','Step 7 · 再进入Efinity综合，不能用综合成功替代功能验证。'],
 'W=8,H=6有效裁剪输出应恰好24个像素，中心坐标范围x=1..6、y=1..4。',['顶层接错端口或valid。','复位后旧数据有效。'],['逐级打印同一中心坐标。','检查输出计数与尾部排空。'],'提交你自己的顶层、接口表和整图测试日志，说明哪些团队接口尚未确认。',['模块单测先通过','整图数量/顺序/坐标一致','复位与帧边界有策略'],['RGB → Gray','行缓存 → Window','Sobel → Threshold','裁剪边缘 / 标签']);
D['18']={goal:'用确定性向量与逐像素比较证明算法核行为，不依赖“看起来像边缘”。',sections:[['WHY · 视觉很难发现少一个像素','错一行、阈值比较差一个等号或漏掉尾像素，图像仍可能呈现轮廓。精确验证同时检查值、数量、顺序和坐标。Python Golden与RTL必须使用同一输入文件、阈值、边界与舍入规格，而不是各自读取不同预处理图片。'],['WHAT · 三份文件与一个比较器','input.hex按行优先提供RGB888；expected.txt由冻结的整数Golden生成；rtl.txt必须由真实仿真器运行你的RTL后输出。比较器输出第一处不一致的索引及预期/实际坐标和值，并以非零退出码表示失败。空文件、X/Z、漏像素和额外像素也失败，不能只zip两份文件后忽略尾部差异。'],['HOW · 测试台采样要避开竞态','在下降沿驱动输入，在上升沿非阻塞赋值更新后观察输出。只在pixel_out_valid高时写文件，包含原图中心坐标。为整个仿真设置超时，并在最后输入后继续时钟直到预期数量输出或超时；不能输入结束立刻$finish。'],['验证分层','依次比较gray、三行同列、九点窗口、Gx/Gy、mag、edge与valid标签。浮点参考用于算法取舍，不参加RTL精确一致门槛。先常量和阶跃，再随机小图，最后真实图片；注入一个错误确认比较器确实能发现。']],code:{name:'SystemVerilog测试台片段 · 仅仿真，不可综合',text:'integer fd;\ninitial fd=$fopen("rtl.txt","w");\nalways @(posedge clk) begin\n  #1; // 仅测试台：避开非阻塞更新采样竞态\n  if (rst_n && pixel_out_valid)\n    $fdisplay(fd,"%0d %0d %0d",center_x,center_y,pixel_out);\nend\n// 补充驱动、计数断言、超时、最后排空与$fclose。'},example:'预期24行、实际23行应FAIL count；值一样但坐标错也FAIL。不能将expected复制成rtl再把PASS作为硬件证据。',downloads:true,diagram:['相同 Test Image','Python整数Golden / RTL仿真','expected.txt / rtl.txt','逐像素 Comparator → PASS / FAIL'],errors:['未等待流水线排空。','只比较显示图。','忽略X/Z或多余行。'],debug:['定位第一处不匹配。','先看其上一级同一坐标。','核对延迟与有效计数。'],exercise:'运行比较器的成功、值错、少一行、顺序错、X值五种场景，保存日志；然后比较真实RTL转储。',checkpoint:['RTL输出来自真实仿真','计数和坐标检查齐全','失败注入有效']};
D['19']={goal:'从第一处数值偏差逐级定位，而不是盲目修改整条流水线。',sections:[['WHY · 从最早错误开始','最后黑白图不对可能来自输入顺序、灰度、窗口、算术或valid。一次同时改多处很难知道哪处起作用。保存失败的固定输入和版本，定位第一个错误中心坐标，然后从输入到输出逐段寻找第一次偏差。'],['WHAT · 固定排查顺序','先确认RGB输入和帧宽，再确认gray；检查行缓存同列值、3×3九点；随后Gx、Gy、幅值、阈值；最后核对valid与输出坐标。如果数值正确但坐标错，优先检查标签延迟；每行开头错则优先检查行界和初始有效区。'],['HOW · 把症状变成假设','整图偏一列可能是RAM响应标签少延迟一拍；只有负梯度错可能是signed类型；阈值附近错可能是>和>=；每帧前两行有垃圾可能是填充状态未清；随机气泡后才错常来自计数和valid不同节奏。每个假设用最小输入验证，不靠反复调阈值遮掩问题。'],['学习与比赛分开记录','复制并修改一个缓存可能让项目模块可用，但你未必能解释读写冲突。日志同时写“今天推进了什么”和“真正理解了什么”，技能矩阵不自动提升到MASTERED。修复后重跑旧用例并保存证据，才能避免换了一个bug。']],code:{name:'Python · 第一处偏差的记录格式',text:'failure = {"stage":"window", "x":1, "y":2,\n           "expected":[5,6,7,10,11,12,15,16,17],\n           "actual":"填入真实转储", "hypothesis":"标签少延迟一拍"}\n# 一次只改变一个假设，重新运行相同输入。'},example:'阶跃图正常但随机图失败，不代表噪声太大；随机图可能暴露错位和算术错误，必须先逐值核对。',diagram:['RGB → Gray','Line Buffer → Window','Gx / Gy → Magnitude','Threshold → Valid / Output'],errors:['把阈值调高隐藏错误。','修改Golden以迎合错误RTL。'],debug:['保存最小失败向量。','只改一处再回归。','同时记录源码版本。'],exercise:'为“每行前两个输出错误、其余正确”写三个可能原因，设计最小实验区分它们。',checkpoint:['固定复现步骤','找到首错节点','修复后回归']};
D['20']={goal:'把已验证的通用算法核放入实际Efinix目标工程，核对工具证据与板级边界。',sections:[['CONCEPT · 软件只是实现概念的一种方式','Efinity是Efinix器件的开发环境，提供综合、布局布线、时序/资源分析以及生成配置等流程。算法、signed位宽、valid和行缓存概念不随IDE变化。本页是厂商适配层；修改界面操作说明不应重写Golden或通用FPGA课程。'],['工程输入必须来自实际团队条件','向队友确认板卡、完整器件/封装/速度等级、工具版本、时钟来源、I/O电压、顶层和复位。先创建对应目标工程，把可综合RTL放入设计源，测试台单独保留。未确定器件时可以继续外部仿真，不能猜一个器件后声称能上板。'],['时序约束和引脚接口分开','Efinity常用SDC描述时钟等时序意图；引脚、I/O与外围资源按目标器件的接口设计流程和官方文档配置。不要把AMD XDC的PACKAGE_PIN/IOSTANDARD指令直接粘贴过来。查看工具识别的时钟、未约束路径、存储推断和关键警告，正slack也需要完整约束作为前提。'],['CURRENT TOOL UI STEPS · 版本待记录','这里不伪造按钮位置。按所装版本的用户指南依次找到工程创建、RTL源、顶层/目标设置、综合、实现、报告、配置生成和Programmer。界面可能使用Interface Designer或版本对应的接口流程；用官方安装目录文档核对。下面逐步记录实际入口、截图路径和结果，后续仅更新这份适配说明。']],code:{name:'SDC意图片段 · 需实际频率，不是可直接用的板级文件',text:'# T_NS = 1000 / 实际板载时钟MHz\n# create_clock -name algorithm_clk -period <T_NS> [get_ports clk]\n# 其余I/O预算、域边界和物理接口设置来自团队确认资料。\n# 不填猜测引脚，不用宽泛例外屏蔽所有时序失败。'},steps:['记录Efinity版本、官方指南版本和完整目标器件。','建工程、加入RTL、设置设计顶层；确认测试台没有进入综合。','应用经过确认的时序约束与器件接口/引脚设置。','运行综合：检查未解析模块、锁存器、截断警告与RAM/资源映射。','运行实现：检查时钟覆盖、建立/保持、资源与关键设计规则。','只有报告问题解释清楚后才生成配置；配置格式按器件与工具版本确定。','使用官方Programmer与匹配硬件连接上板，保存日志和可复现步骤；网站不会代为执行。'],example:'综合成功只说明工具能构建网表；它不证明Sobel数值正确、外部时序正确或板卡连接正确。',diagram:['已验证 RTL + 真实目标','Efinity 综合 / 实现','时序 / 资源 / 约束覆盖','配置 → Programmer → 板测'],errors:['错Part/封装。','测试台进入综合。','照搬其他厂商引脚命令。'],debug:['从第一条重要错误开始。','确认顶层与文件列表。','查看存储是否按预期推断。'],exercise:'填写实际工具版本与每一步证据路径，未运行的标为BLOCKED并写具体缺少的条件。',checkpoint:['器件和时钟有来源','实现后报告已看','外部工具执行状态真实'],sources:[['Efinix官方Efinity与文档入口','https://www.efinixinc.com/products-efinity.html'],['Efinix官方文档目录','https://www.efinixinc.com/support/docs.php']]};
D['21']={goal:'与队友冻结算法核边界，并交付可复现的摄像头到显示演示。',sections:[['MY MODULE · 算法与算法硬件核心','你的主要交付是软件参考、整数数学行为、位宽与流水线、RGB→Gray、行缓存/窗口、Sobel、阈值、测试台和逐像素验证。摄像头接口和HDMI/VGA实现是团队依赖，不应成为你当前学习的中心；但你需要协助确认接口并排查边界。'],['SYSTEM INTERFACE · 对接前先写表','双方确认像素格式、宽高、像素时钟、valid/ready、SOF/EOL、复位极性与同步方式、数据延迟、输出裁剪/补边与颜色格式。只说“8位图像”不够：有的显示端要整幅W×H光栅，而算法只产生(W-2)×(H-2)个有效中心，必须有人负责补边与同步重建。'],['TEAM DEPENDENCY · 系统依赖不等于你没学会算法','摄像头输入不稳定、显示时序不匹配、跨时钟FIFO缺失都可能阻塞上板，但不妨碍继续完成单域算法验证。把阻塞责任、需要的信号和下一次确认写清楚。来自不同域的数据不能逐位两级同步后直接当作完整像素。'],['MUST FINISH 与演示','先完成稳定摄像头输入（团队）、灰度/行缓存/3×3Sobel/阈值（你）、稳定显示（团队）、完整RTL/约束/可复现板上演示（共同）。按双方约定时长、场景、复位次数和帧率目标记录实际结果，不虚构官方门槛。阈值调节、彩色叠加和中值滤波等要等基线稳定后再排。']],code:{name:'接口合同示例 · 需要团队确认后填写',text:'pixel_format: RGB888 / 待确认\nclock_domain: algorithm_clk / 与camera关系待确认\naccept: pixel_valid (基线无背压)\nborder: valid crop; display padding owner: 待确认\noutput: edge8 + center_x/y + valid\nreset: synchronous active-low within algorithm domain'},example:'算法仿真通过但画面错位时，检查显示端是否把裁剪宽W-2当作W，或SOF/EOL未跟随流水延迟。',diagram:['TEAM · Camera / Interface','SYSTEM · RGB + valid + sync','MY · Gray → Buffer → Sobel → Threshold','TEAM · Display → HDMI / VGA'],errors:['不约定输出边界。','跨域与复位责任无人负责。'],debug:['先接团队测试图发生器。','再接真实摄像头。','先验证固定阈值与完整回归。'],exercise:'和队友填写下方系统接口表，记录待确认项、负责人和协议版本；准备从空目录重建的交付README。',checkpoint:['接口双方确认','依赖与责任明确','演示与限制可复现']};
})(window);
